---
phase: 01-核心数据骨架
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - project.config.json
  - tsconfig.json
  - jest.config.cjs
  - miniprogram/app.ts
  - miniprogram/app.json
  - miniprogram/app.wxss
  - miniprogram/env.d.ts
  - miniprogram/pages/workbench/index.json
  - miniprogram/pages/workbench/index.ts
  - miniprogram/pages/workbench/index.wxml
  - miniprogram/pages/workbench/index.wxss
  - tests/helpers/mock-wx.ts
  - tests/helpers/mock-cloud.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "仓库可以被微信开发者工具识别为原生小程序工程。"
    - "Phase 1 后续计划可以直接复用 TypeScript、Jest 和云函数测试基座。"
    - "应用入口只负责启动与会话引导，不夹带业务页面逻辑。"
  artifacts:
    - "package.json: Phase 1 依赖与脚本"
    - "project.config.json: 小程序与云函数根目录配置"
    - "tests/helpers/mock-wx.ts: 小程序 API 测试桩"
  key_links:
    - "project.config.json -> miniprogram/ via miniprogramRoot"
    - "jest.config.cjs -> tests/helpers/* via module setup"
---

<objective>
建立原生微信小程序工程骨架与可复用测试基座。

Purpose: 先把工程、脚本和测试桩搭稳，后续登录、领域模型、云函数与页面计划都能直接落在一致底座上。
Output: 小程序根配置、TypeScript 与 Jest 配置、应用入口文件、工作台占位页与测试辅助桩。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化原生微信小程序 TypeScript 工程</name>
  <files>package.json, project.config.json, tsconfig.json, miniprogram/app.ts, miniprogram/app.json, miniprogram/app.wxss, miniprogram/env.d.ts, miniprogram/pages/workbench/index.json, miniprogram/pages/workbench/index.ts, miniprogram/pages/workbench/index.wxml, miniprogram/pages/workbench/index.wxss</files>
  <read_first>.planning/phases/01-核心数据骨架/1-CONTEXT.md, .planning/ROADMAP.md, .planning/research/SUMMARY.md</read_first>
  <action>创建 `miniprogram/` 作为小程序根目录，并在 `package.json` 中写入 `typescript`、`miniprogram-api-typings`、`tdesign-miniprogram`、`jest`、`ts-jest`、`eslint`、`prettier` 依赖与 `typecheck`、`test`、`lint` 脚本。`project.config.json` 固定 `miniprogramRoot: "miniprogram/"`、`cloudfunctionRoot: "cloudfunctions/"`、`compileType: "miniprogram"`。`miniprogram/app.json` 在本计划只注册 `pages/workbench/index` 这个已随本计划创建的最小落点页；同步创建 `pages/workbench/index` 占位文件，仅展示工程已初始化状态，禁止在本计划引用 `pages/auth/index` 等未来页面。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `project.config.json` contains `"miniprogramRoot": "miniprogram/"`
    - `project.config.json` contains `"cloudfunctionRoot": "cloudfunctions/"`
    - `miniprogram/app.json` contains `"pages/workbench/index"`
    - `miniprogram/app.json` does not contain `"pages/auth/index"`
    - `miniprogram/pages/workbench/index.wxml` contains `工程已初始化`
  </acceptance_criteria>
  <done>工程目录、脚本和最小可落地路由已经具备，后续计划可以在不依赖未来页面的前提下增量实现认证与业务功能。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Jest 与云函数测试辅助桩</name>
  <files>jest.config.cjs, tests/helpers/mock-wx.ts, tests/helpers/mock-cloud.ts, package.json</files>
  <read_first>.planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, .planning/phases/01-核心数据骨架/1-CONTEXT.md</read_first>
  <action>创建 `jest.config.cjs`，让 `tests/**` 下的 Phase 1 用例可直接运行。新增 `tests/helpers/mock-wx.ts` 封装 `wx.getStorageSync`、`wx.setStorageSync`、`wx.cloud.callFunction` 等 mock；新增 `tests/helpers/mock-cloud.ts` 封装云数据库与 `getWXContext` 的测试桩。不要在这里实现任何业务逻辑，只提供后续计划可复用的测试基础设施。</action>
  <verify>
    <automated>npm test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `jest.config.cjs` contains `tests/helpers`
    - `tests/helpers/mock-wx.ts` contains `wx.cloud.callFunction`
    - `tests/helpers/mock-cloud.ts` contains `getWXContext`
  </acceptance_criteria>
  <done>后续登录、领域和云函数测试都能复用同一套小程序与云能力测试桩。</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- --runInBand`
</verification>

<success_criteria>
工程基座已完成，后续计划无需再重复处理项目初始化和测试环境问题。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-01-SUMMARY.md`
</output>
