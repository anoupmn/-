---
phase: 01-核心数据骨架
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - project.config.json
  - tsconfig.json
  - jest.config.cjs
  - miniprogram/app.ts
  - miniprogram/app.json
  - miniprogram/app.wxss
  - miniprogram/env.d.ts
  - miniprogram/pages/auth/index.json
  - miniprogram/pages/auth/index.ts
  - miniprogram/pages/auth/index.wxml
  - miniprogram/pages/auth/index.wxss
  - miniprogram/services/auth.ts
  - miniprogram/utils/storage.ts
  - cloudfunctions/login/index.ts
  - cloudfunctions/shared/constants/collections.ts
  - tests/auth/auth-service.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup:
  - service: wechat-mini-program
    why: "真机验证微信登录需要有效 AppID 和开发者工具项目绑定。"
    env_vars:
      - name: WECHAT_APP_ID
        source: "微信公众平台 -> 开发 -> 开发设置"
    dashboard_config:
      - task: "创建并绑定 CloudBase 环境"
        location: "微信开发者工具 -> 云开发"
  - service: cloudbase
    why: "登录云函数需要落盘 landlord 用户会话与 openid。"
    env_vars:
      - name: CLOUDBASE_ENV_ID
        source: "微信开发者工具 -> 云开发环境 ID"
must_haves:
  truths:
    - "用户首次打开小程序会进入认证页并能触发微信登录。"
    - "同一设备再次打开小程序时会优先读取本地会话，不要求重复登录。"
    - "云函数以 openid 作为唯一 landlord 身份来源。"
  artifacts:
    - "miniprogram/services/auth.ts: 登录与会话恢复服务"
    - "cloudfunctions/login/index.ts: openid 登录云函数"
    - "miniprogram/pages/auth/index.ts: 认证页入口"
  key_links:
    - "miniprogram/app.ts -> miniprogram/services/auth.ts via bootstrapAuthSession"
    - "miniprogram/services/auth.ts -> cloudfunctions/login/index.ts via wx.cloud.callFunction"
---

<objective>
建立 Phase 1 的原生微信小程序工程、测试基座和登录会话能力。

Purpose: 先把工程入口和唯一身份来源搭稳，后续房源、租约和录入页面才能直接复用登录态与云能力。
Output: 可运行的小程序脚手架、认证页、登录云函数、会话持久化服务与基础测试配置。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化微信小程序 TypeScript 工程与测试基座</name>
  <files>package.json, project.config.json, tsconfig.json, jest.config.cjs, miniprogram/app.ts, miniprogram/app.json, miniprogram/app.wxss, miniprogram/env.d.ts</files>
  <action>创建原生微信小程序目录 `miniprogram/` 和云函数目录 `cloudfunctions/`。`package.json` 写入 `typescript`、`miniprogram-api-typings`、`tdesign-miniprogram`、`jest`、`ts-jest`、`eslint`、`prettier` 依赖与 `lint`、`test`、`typecheck` 脚本；`project.config.json` 使用 `miniprogramRoot: "miniprogram/"`、`cloudfunctionRoot: "cloudfunctions/"`、`compileType: "miniprogram"`；`app.json` 注册 `pages/auth/index` 为首屏并预留后续页面路由；`app.ts` 只放启动和会话初始化，不引入业务逻辑。</action>
  <verify>
    <automated>npm install && npm run typecheck</automated>
  </verify>
  <done>项目能被微信开发者工具识别为原生小程序工程，TypeScript 和 Jest 基座可执行。</done>
</task>

<task type="auto">
  <name>Task 2: 实现微信登录云函数与本地会话持久化</name>
  <files>miniprogram/pages/auth/index.json, miniprogram/pages/auth/index.ts, miniprogram/pages/auth/index.wxml, miniprogram/pages/auth/index.wxss, miniprogram/services/auth.ts, miniprogram/utils/storage.ts, cloudfunctions/login/index.ts, cloudfunctions/shared/constants/collections.ts, tests/auth/auth-service.spec.ts, miniprogram/app.ts</files>
  <action>实现唯一 landlord 登录流。`cloudfunctions/login/index.ts` 使用 `cloud.getWXContext()` 读取 `OPENID`，写入 `landlord_users` 集合，字段固定为 `openid`、`lastLoginAt`、`displayName`、`role: "landlord"`；`miniprogram/services/auth.ts` 暴露 `bootstrapAuthSession()`、`loginAsLandlord()`、`clearSession()`，本地缓存 key 固定为 `RZB_SESSION_KEY`；`miniprogram/pages/auth/index.ts` 提供 `handleWechatLogin`，成功后 `wx.reLaunch` 到 `/pages/units/index`，失败时展示明确错误文案；`app.ts` 的 `onLaunch` 先尝试 `bootstrapAuthSession()`，本地无会话时停留在认证页。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/auth/auth-service.spec.ts</automated>
  </verify>
  <done>用户身份来源统一为微信 openid，本地存储可恢复同设备会话，认证页可触发云函数登录且无独立账号密码分支。</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- --runTestsByPath tests/auth/auth-service.spec.ts`
</verification>

<success_criteria>
用户可以完成微信登录并保留登录状态，后续所有 Phase 1 页面和云函数都能复用同一会话入口。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-01-SUMMARY.md`
</output>
