---
phase: 01-核心数据骨架
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - cloudfunctions/shared/constants/statuses.ts
  - cloudfunctions/shared/schemas/asset.ts
  - cloudfunctions/shared/schemas/room.ts
  - cloudfunctions/shared/schemas/tenant.ts
  - cloudfunctions/shared/schemas/lease.ts
  - cloudfunctions/shared/calculators/rentable-unit.ts
  - cloudfunctions/shared/calculators/lease-lifecycle.ts
  - cloudfunctions/shared/repositories/asset-repository.ts
  - cloudfunctions/shared/repositories/room-repository.ts
  - cloudfunctions/shared/repositories/tenant-repository.ts
  - cloudfunctions/shared/repositories/lease-repository.ts
  - tests/domain/lease-lifecycle.spec.ts
  - tests/domain/rentable-unit-summary.spec.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - LEASE-02
  - LEASE-03
  - LEASE-04
  - LEASE-05
must_haves:
  truths:
    - "整租房源和分租房源都能落到同一个 房源 -> 房间 -> 租约 模型。"
    - "同一个可出租单元任意时刻最多只有一个生效中租约，但允许存在未来租约。"
    - "当前租户与当前状态由生效中租约推导，不存在第二份手工维护的当前租户字段。"
    - "结束租约后仍可保留完整历史租约和历任租户关系。"
  artifacts:
    - path: "cloudfunctions/shared/schemas/lease.ts"
      provides: "租约结构与状态字段"
      contains: "activeLease"
    - path: "cloudfunctions/shared/calculators/lease-lifecycle.ts"
      provides: "当前状态、未来租约、结束租约后的空置/待入住推导"
      contains: "assertSingleActiveLease"
    - path: "cloudfunctions/shared/calculators/rentable-unit.ts"
      provides: "列表摘要字段推导"
      contains: "currentTenantName"
  key_links:
    - from: "cloudfunctions/shared/schemas/asset.ts"
      to: "cloudfunctions/shared/schemas/room.ts"
      via: "默认整租房间 whole-unit room"
      pattern: "isWholeUnitDefault"
    - from: "cloudfunctions/shared/schemas/lease.ts"
      to: "cloudfunctions/shared/calculators/lease-lifecycle.ts"
      via: "active/future/history 租约规则"
      pattern: "assertSingleActiveLease"
    - from: "cloudfunctions/shared/calculators/rentable-unit.ts"
      to: "cloudfunctions/shared/schemas/tenant.ts"
      via: "currentTenantName derived from active lease"
      pattern: "currentTenantName"
---

<objective>
定义 Phase 1 的领域契约、生命周期规则和可复用仓储边界。

Purpose: 先把核心结构和不可妥协的业务规则写死，避免后续 UI 和云函数围绕错误模型继续生长。
Output: 资产、房间、租户、租约 schema，整租默认房间规则，租约生命周期计算器，以及仓储接口。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-核心数据骨架/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 以测试先行定义资产-房间-租约领域契约与生命周期规则</name>
  <files>cloudfunctions/shared/constants/statuses.ts, cloudfunctions/shared/schemas/asset.ts, cloudfunctions/shared/schemas/room.ts, cloudfunctions/shared/schemas/tenant.ts, cloudfunctions/shared/schemas/lease.ts, cloudfunctions/shared/calculators/rentable-unit.ts, cloudfunctions/shared/calculators/lease-lifecycle.ts, tests/domain/lease-lifecycle.spec.ts, tests/domain/rentable-unit-summary.spec.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-核心数据骨架/1-CONTEXT.md, cloudfunctions/shared/schemas/asset.ts, cloudfunctions/shared/schemas/lease.ts, tests/domain/lease-lifecycle.spec.ts</read_first>
  <acceptance_criteria>
    - `cloudfunctions/shared/schemas/room.ts` contains `isWholeUnitDefault`
    - `cloudfunctions/shared/schemas/lease.ts` contains `roomId`
    - `cloudfunctions/shared/calculators/lease-lifecycle.ts` contains `assertSingleActiveLease`
    - `cloudfunctions/shared/calculators/rentable-unit.ts` contains `currentTenantName`
    - `tests/domain/lease-lifecycle.spec.ts` contains `allows future lease`
    - `tests/domain/rentable-unit-summary.spec.ts` contains `derives current tenant from active lease`
  </acceptance_criteria>
  <action>按 D-01 到 D-04、D-10 到 D-14 固化领域契约。`asset.ts` 固定字段 `id`、`name`、`rentalMode`(`whole`|`room`)、`address`、`notes`；`room.ts` 固定字段 `assetId`、`name`、`rentableType`、`isWholeUnitDefault`；`lease.ts` 固定字段 `roomId`、`tenantIds`、`startDate`、`endDate`、`billingCycleDays`、`rentAmount`、`depositAmount`、`status`(`draft`|`active`|`future`|`ended`)；`lease-lifecycle.ts` 必须实现 `assertSingleActiveLease`、`deriveLeaseStatus`、`closeLeaseAndDeriveUnitStatus`；`rentable-unit.ts` 必须输出列表基础摘要 `displayName`、`currentStatus`、`currentTenantName`、`nextReceivableDate`、`nextReceivableAmount`、`isAbnormal`、`statusMetricValue`。先写两个 Jest 测试文件，覆盖整租默认房间、未来租约允许、单房间单 active lease、结束租约保留历史、当前租户必须从 active lease 推导这五类行为，再补实现。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/domain/lease-lifecycle.spec.ts tests/domain/rentable-unit-summary.spec.ts</automated>
  </verify>
  <done>领域规则已经通过自动化测试锁定，整租与分租共用一套 schema，且不存在独立当前租户事实字段。</done>
</task>

<task type="auto">
  <name>Task 2: 建立领域仓储边界与后续云函数复用的数据访问接口</name>
  <files>cloudfunctions/shared/repositories/asset-repository.ts, cloudfunctions/shared/repositories/room-repository.ts, cloudfunctions/shared/repositories/tenant-repository.ts, cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/schemas/asset.ts, cloudfunctions/shared/schemas/room.ts, cloudfunctions/shared/schemas/tenant.ts, cloudfunctions/shared/schemas/lease.ts</files>
  <read_first>.planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, cloudfunctions/shared/repositories/asset-repository.ts, cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/schemas/lease.ts</read_first>
  <action>创建云函数共享仓储接口，避免后续页面直接拼数据库结构。`asset-repository.ts` 暴露 `createAssetWithDefaultRoomForWholeMode` 与 `updateAsset`; `room-repository.ts` 暴露 `createRoom`、`updateRoom`、`listRoomsByAsset`; `tenant-repository.ts` 暴露 `createTenant`、`updateTenant`、`listTenantsByKeyword`; `lease-repository.ts` 暴露 `createLease`、`updateLease`、`endLease`、`listLeasesByRoom`。这些仓储接口必须显式接收前一任务定义的 schema 类型，并在注释中声明：禁止写入任何 `currentTenantId` 或手工 `occupancyStatus` 字段，状态一律交给计算器推导。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/shared/repositories/asset-repository.ts` contains `createAssetWithDefaultRoomForWholeMode`
    - `cloudfunctions/shared/repositories/lease-repository.ts` contains `endLease`
    - `cloudfunctions/shared/repositories/lease-repository.ts` contains `currentTenantId`
    - `cloudfunctions/shared/repositories/lease-repository.ts` contains `MUST NOT persist currentTenantId`
    - `cloudfunctions/shared/repositories/room-repository.ts` contains `listRoomsByAsset`
  </acceptance_criteria>
  <done>后续云函数和页面服务已拥有明确的仓储接口，领域规则不会散落到 UI 页面里临时实现。</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --runTestsByPath tests/domain/lease-lifecycle.spec.ts tests/domain/rentable-unit-summary.spec.ts`
- [ ] `npm run typecheck`
- [ ] 默认整租房间与单 active lease 规则均由自动化测试覆盖
</verification>

<success_criteria>

- 所有任务完成
- D-01 到 D-04 与 D-10 到 D-14 已落为代码契约
- 后续执行者可以围绕 schema 和仓储继续实现，不需要重新发明模型
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-02-SUMMARY.md`
</output>
