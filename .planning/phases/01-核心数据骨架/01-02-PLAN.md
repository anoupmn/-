---
phase: 01-核心数据骨架
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - cloudfunctions/shared/constants/statuses.ts
  - cloudfunctions/shared/schemas/asset.ts
  - cloudfunctions/shared/schemas/room.ts
  - cloudfunctions/shared/schemas/tenant.ts
  - cloudfunctions/shared/schemas/lease.ts
  - cloudfunctions/shared/calculators/rentable-unit.ts
  - cloudfunctions/shared/calculators/lease-lifecycle.ts
  - cloudfunctions/shared/repositories/asset-repository.ts
  - cloudfunctions/shared/repositories/room-repository.ts
  - cloudfunctions/shared/repositories/tenant-repository.ts
  - cloudfunctions/shared/repositories/lease-repository.ts
  - tests/domain/lease-lifecycle.spec.ts
  - tests/domain/rentable-unit-summary.spec.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - LEASE-02
  - LEASE-03
  - LEASE-04
  - LEASE-05
must_haves:
  truths:
    - "整租房源和分租房源都能落到同一个房源 -> 房间 -> 租约模型。"
    - "同一个可出租单元任意时刻最多只有一个生效中租约，但允许存在未来租约。"
    - "当前租户与当前状态由生效中租约推导，不存在第二份手工维护字段。"
    - "结束租约后仍保留完整历史租约和历任租户关系。"
  artifacts:
    - "cloudfunctions/shared/schemas/*.ts: 资产、房间、租户、租约 schema"
    - "cloudfunctions/shared/calculators/lease-lifecycle.ts: 生命周期规则"
    - "cloudfunctions/shared/repositories/*.ts: 领域仓储边界"
  key_links:
    - "asset.ts -> room.ts via whole-unit 默认房间 per D-03"
    - "lease.ts -> lease-lifecycle.ts via 单 active lease 校验 per D-10~D-14"
    - "rentable-unit.ts -> tenant.ts via 当前租户推导"
---

<objective>
定义 Phase 1 的领域契约、生命周期规则和可复用仓储边界。

Purpose: 先把核心结构和不可妥协的业务规则写死，避免后续 UI 和云函数围绕错误模型继续生长。
Output: 资产、房间、租户、租约 schema，整租默认房间规则，租约生命周期计算器，以及仓储接口。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 以测试先行定义房源-房间-租约领域契约与生命周期规则</name>
  <files>cloudfunctions/shared/constants/statuses.ts, cloudfunctions/shared/schemas/asset.ts, cloudfunctions/shared/schemas/room.ts, cloudfunctions/shared/schemas/tenant.ts, cloudfunctions/shared/schemas/lease.ts, cloudfunctions/shared/calculators/rentable-unit.ts, cloudfunctions/shared/calculators/lease-lifecycle.ts, tests/domain/lease-lifecycle.spec.ts, tests/domain/rentable-unit-summary.spec.ts</files>
  <behavior>
    - Test 1: 整租模式会创建默认 whole-unit 房间，满足 D-01 到 D-04。
    - Test 2: 同一房间拒绝第二个 active lease，但允许 future lease，满足 D-10 与 D-11。
    - Test 3: 结束租约后保留历史，并由 active lease 推导当前租户与状态，满足 D-12 到 D-14。
  </behavior>
  <action>按 D-01 到 D-04、D-10 到 D-14 固化领域契约。`asset.ts` 固定 `rentalMode`；`room.ts` 固定 `isWholeUnitDefault`；`lease.ts` 固定绑定 `roomId` 而不是 `assetId`；`lease-lifecycle.ts` 必须实现 `assertSingleActiveLease`、`deriveLeaseStatus`、`closeLeaseAndDeriveUnitStatus`；`rentable-unit.ts` 必须输出 `displayName`、`currentStatus`、`currentTenantName` 等首屏摘要字段。先写测试，再补实现。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/domain/lease-lifecycle.spec.ts tests/domain/rentable-unit-summary.spec.ts</automated>
  </verify>
  <done>领域规则已经通过自动化测试锁定，整租与分租共用一套 schema，且不存在独立当前租户事实字段。</done>
</task>

<task type="auto">
  <name>Task 2: 建立云函数共享仓储边界</name>
  <files>cloudfunctions/shared/repositories/asset-repository.ts, cloudfunctions/shared/repositories/room-repository.ts, cloudfunctions/shared/repositories/tenant-repository.ts, cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/schemas/asset.ts, cloudfunctions/shared/schemas/room.ts, cloudfunctions/shared/schemas/tenant.ts, cloudfunctions/shared/schemas/lease.ts</files>
  <action>创建云函数共享仓储接口，避免后续页面和云函数直接拼数据库结构。`asset-repository.ts` 暴露 `createAssetWithDefaultRoomForWholeMode` 与 `updateAsset`；`room-repository.ts` 暴露 `createRoom`、`updateRoom`、`listRoomsByAsset`；`tenant-repository.ts` 暴露 `createTenant`、`updateTenant`；`lease-repository.ts` 暴露 `createLease`、`updateLease`、`endLease`、`listLeasesByRoom`。在接口约束里显式写明禁止持久化 `currentTenantId` 或手工 `occupancyStatus`，必须复用上一步计算器。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>后续云函数与页面服务拥有统一的数据访问契约，不需要再次猜测模型边界。</done>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath tests/domain/lease-lifecycle.spec.ts tests/domain/rentable-unit-summary.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
D-01 到 D-04、D-10 到 D-14 已落为代码契约，后续执行者可以直接围绕 schema、calculator 和 repository 实现功能。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-02-SUMMARY.md`
</output>
