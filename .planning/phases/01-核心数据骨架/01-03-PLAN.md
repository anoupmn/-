---
phase: 01-核心数据骨架
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - cloudfunctions/shared/constants/statuses.ts
  - cloudfunctions/shared/schemas/asset.ts
  - cloudfunctions/shared/schemas/room.ts
  - cloudfunctions/shared/schemas/tenant.ts
  - cloudfunctions/shared/schemas/lease.ts
  - cloudfunctions/shared/calculators/rentable-unit.ts
  - cloudfunctions/shared/calculators/lease-lifecycle.ts
  - cloudfunctions/shared/repositories/asset-repository.ts
  - cloudfunctions/shared/repositories/room-repository.ts
  - cloudfunctions/shared/repositories/tenant-repository.ts
  - cloudfunctions/shared/repositories/lease-repository.ts
  - cloudfunctions/quick-entry/index.ts
  - cloudfunctions/assets-save/index.ts
  - cloudfunctions/rooms-save/index.ts
  - cloudfunctions/tenants-save/index.ts
  - cloudfunctions/leases-save/index.ts
  - cloudfunctions/leases-end/index.ts
  - cloudfunctions/rentable-units-list/index.ts
  - cloudfunctions/rentable-unit-detail/index.ts
  - miniprogram/services/quick-entry.ts
  - miniprogram/services/asset.ts
  - miniprogram/services/room.ts
  - miniprogram/services/tenant.ts
  - miniprogram/services/lease.ts
  - miniprogram/services/rentable-unit.ts
  - tests/domain/lease-lifecycle.spec.ts
  - tests/domain/rentable-unit-summary.spec.ts
  - tests/cloud/quick-entry.spec.ts
  - tests/cloud/entity-save.spec.ts
  - tests/cloud/rentable-units-list.spec.ts
  - tests/cloud/rentable-unit-detail.spec.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - LEASE-01
  - LEASE-02
  - LEASE-03
  - LEASE-04
  - LEASE-05
  - LIST-01
  - IMPT-01
must_haves:
  truths:
    - "整租与分租都落在房源 -> 房间 -> 租约模型上。"
    - "房源必须带有 `rentalMode`，并驱动 whole-unit 默认房间规则。"
    - "快速录入、分步维护、列表查询与详情查询都复用同一套领域规则。"
    - "同一可出租单元同一时刻只允许一个 active lease，但允许 future lease。"
  artifacts:
    - "cloudfunctions/shared/schemas/asset.ts: 含 rentalMode 的房源 schema"
    - "cloudfunctions/shared/calculators/lease-lifecycle.ts: 生命周期规则"
    - "cloudfunctions/quick-entry/index.ts: 一口气建档入口"
    - "cloudfunctions/rentable-units-list/index.ts: 列表摘要查询"
  key_links:
    - "asset.ts -> room.ts via whole-unit 默认单元"
    - "lease.ts -> lease-lifecycle.ts via 单 active lease 校验"
    - "miniprogram/services/quick-entry.ts -> cloudfunctions/quick-entry/index.ts"
    - "miniprogram/services/rentable-unit.ts -> cloudfunctions/rentable-units-list/index.ts"
---

<objective>
固化 Phase 1 的领域模型，并把手动录入与查询所需的后端通道一次性搭好。

Purpose: 先把最容易返工的业务结构和服务入口写死，前端页面层只负责接入，不再一边做页面一边猜业务规则。
Output: 领域 schema、生命周期计算器、共享仓储接口、快速录入/分步维护/列表/详情云函数，以及对应前端 service。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用测试锁定核心领域契约与生命周期规则</name>
  <files>cloudfunctions/shared/constants/statuses.ts, cloudfunctions/shared/schemas/asset.ts, cloudfunctions/shared/schemas/room.ts, cloudfunctions/shared/schemas/tenant.ts, cloudfunctions/shared/schemas/lease.ts, cloudfunctions/shared/calculators/rentable-unit.ts, cloudfunctions/shared/calculators/lease-lifecycle.ts, tests/domain/lease-lifecycle.spec.ts, tests/domain/rentable-unit-summary.spec.ts</files>
  <read_first>.planning/phases/01-核心数据骨架/1-CONTEXT.md, .planning/research/PITFALLS.md, .planning/REQUIREMENTS.md</read_first>
  <action>按 D-01 到 D-04 与 D-10 到 D-14 编码领域规则。`asset.ts` 必须声明 `rentalMode: 'whole' | 'room'`；`room.ts` 必须声明 `isWholeUnitDefault`；`lease.ts` 只能绑定 `roomId`；`lease-lifecycle.ts` 必须实现 `assertSingleActiveLease`、`deriveLeaseStatus`、`closeLeaseAndDeriveUnitStatus`；`rentable-unit.ts` 必须输出 `displayName`、`currentStatus`、`currentTenantName` 等 D-08 摘要字段。先写失败测试，再补实现。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/domain/lease-lifecycle.spec.ts tests/domain/rentable-unit-summary.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/shared/schemas/asset.ts` contains `rentalMode`
    - `cloudfunctions/shared/schemas/room.ts` contains `isWholeUnitDefault`
    - `cloudfunctions/shared/calculators/lease-lifecycle.ts` contains `assertSingleActiveLease`
    - `tests/domain/lease-lifecycle.spec.ts` contains `future lease`
  </acceptance_criteria>
  <done>整租/分租模型与租约生命周期规则已经被自动化测试锁定，后续任务无需再推断业务边界。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现快速录入、分步维护、列表与详情的后端通道</name>
  <files>cloudfunctions/shared/repositories/asset-repository.ts, cloudfunctions/shared/repositories/room-repository.ts, cloudfunctions/shared/repositories/tenant-repository.ts, cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/quick-entry/index.ts, cloudfunctions/assets-save/index.ts, cloudfunctions/rooms-save/index.ts, cloudfunctions/tenants-save/index.ts, cloudfunctions/leases-save/index.ts, cloudfunctions/leases-end/index.ts, cloudfunctions/rentable-units-list/index.ts, cloudfunctions/rentable-unit-detail/index.ts, miniprogram/services/quick-entry.ts, miniprogram/services/asset.ts, miniprogram/services/room.ts, miniprogram/services/tenant.ts, miniprogram/services/lease.ts, miniprogram/services/rentable-unit.ts, tests/cloud/quick-entry.spec.ts, tests/cloud/entity-save.spec.ts, tests/cloud/rentable-units-list.spec.ts, tests/cloud/rentable-unit-detail.spec.ts</files>
  <read_first>.planning/phases/01-核心数据骨架/01-03-PLAN.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, .planning/phases/01-核心数据骨架/1-CONTEXT.md</read_first>
  <action>创建共享仓储接口，并在此基础上把 Phase 1 的所有后端入口一次性打通。`asset-repository.ts` 支持 `createAssetWithDefaultRoomForWholeMode` 与 `updateAsset`；`room-repository.ts` 支持 `createRoom`、`updateRoom`、`listRoomsByAsset`；`tenant-repository.ts` 支持 `createTenant`、`updateTenant`；`lease-repository.ts` 支持 `createLease`、`updateLease`、`endLease`、`listLeasesByRoom`。实现 `quick-entry` 云函数接收 `asset`、`rooms`、`tenant`、`lease` 与 `mode: 'quick-entry'`，整租时强制创建默认 whole-unit 单元；实现 `assets-save`、`rooms-save`、`tenants-save`、`leases-save`、`leases-end` 五个分步维护入口；实现 `rentable-units-list` 与 `rentable-unit-detail` 两个查询入口，列表输出 D-08 的首屏字段，详情输出 `asset`、`room`、`activeLease`、`leaseHistory`、`tenantHistory`。所有客户端 service 只包装 `wx.cloud.callFunction`，禁止页面直接操作集合，也禁止持久化 `currentTenantId` 或手工房态字段。先补针对 quick-entry、单实体保存、列表与详情查询的测试，再补实现。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/quick-entry.spec.ts tests/cloud/entity-save.spec.ts tests/cloud/rentable-units-list.spec.ts tests/cloud/rentable-unit-detail.spec.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/quick-entry/index.ts` contains `mode: 'quick-entry'`
    - `cloudfunctions/assets-save/index.ts` contains `rentalMode`
    - `cloudfunctions/rooms-save/index.ts` contains `createRoom`
    - `cloudfunctions/leases-end/index.ts` contains `endLease`
    - `cloudfunctions/rentable-units-list/index.ts` contains `nextReceivableAmount`
    - `cloudfunctions/rentable-unit-detail/index.ts` contains `tenantHistory`
    - `miniprogram/services/quick-entry.ts` contains `submitQuickEntry`
    - `miniprogram/services/rentable-unit.ts` contains `getRentableUnitDetail`
  </acceptance_criteria>
  <done>Phase 1 所需的后端与 service 通道已经完整可用，页面层可以直接接入快速录入、分步维护、列表与详情闭环。</done>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath tests/domain/lease-lifecycle.spec.ts tests/domain/rentable-unit-summary.spec.ts`
- `npm test -- --runTestsByPath tests/cloud/quick-entry.spec.ts tests/cloud/entity-save.spec.ts tests/cloud/rentable-units-list.spec.ts tests/cloud/rentable-unit-detail.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
ASST-03、IMPT-01 与 LIST-01 所需的核心后端能力已经完整落地，前端只需接入既有服务而不需要再发明业务规则。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-03-SUMMARY.md`
</output>
