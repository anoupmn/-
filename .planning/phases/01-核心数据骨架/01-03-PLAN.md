---
phase: 01-核心数据骨架
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - cloudfunctions/quick-entry/index.ts
  - cloudfunctions/assets-save/index.ts
  - cloudfunctions/rooms-save/index.ts
  - cloudfunctions/tenants-save/index.ts
  - cloudfunctions/leases-save/index.ts
  - cloudfunctions/leases-end/index.ts
  - miniprogram/services/quick-entry.ts
  - miniprogram/services/asset.ts
  - miniprogram/services/room.ts
  - miniprogram/services/tenant.ts
  - miniprogram/services/lease.ts
  - tests/cloud/quick-entry.spec.ts
  - tests/cloud/entity-save.spec.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - LEASE-01
  - LEASE-02
  - LEASE-03
  - LEASE-04
  - IMPT-01
must_haves:
  truths:
    - "快速录入可以一次性写入房源、房间、当前租户和当前租约。"
    - "分步维护可以分别保存房源、房间、租户、租约，并支持结束租约。"
    - "前端快速录入走 dedicated one-shot wrapper，不拆成多个 save 调用。"
  artifacts:
    - "cloudfunctions/quick-entry/index.ts: 首次建档 one-shot 写入入口"
    - "cloudfunctions/rooms-save/index.ts: 房间分步维护入口"
    - "miniprogram/services/quick-entry.ts: quick-entry 专用客户端包装"
  key_links:
    - "miniprogram/services/quick-entry.ts -> cloudfunctions/quick-entry/index.ts via wx.cloud.callFunction per D-06"
    - "miniprogram/services/room.ts -> cloudfunctions/rooms-save/index.ts via 分步维护"
    - "cloudfunctions/leases-end/index.ts -> lease-repository.ts via endLease"
---

<objective>
实现手动录入所需的写入云函数入口和客户端服务包装。

Purpose: 把领域契约变成真实可调用的保存与结束租约能力，并把快速录入和分步维护严格分成两条路径。
Output: 快速录入云函数、分步维护云函数、结束租约云函数，以及对应前端服务。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/phases/01-核心数据骨架/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现快速录入 one-shot 云函数与专用客户端 wrapper</name>
  <files>cloudfunctions/quick-entry/index.ts, miniprogram/services/quick-entry.ts, tests/cloud/quick-entry.spec.ts</files>
  <behavior>
    - Test 1: 一个请求能创建房源、房间、租户、当前租约，满足 D-05 与 D-06。
    - Test 2: 整租模式自动创建默认 whole-unit 房间。
    - Test 3: 请求不得拆成多个 asset/tenant/lease save 调用。
  </behavior>
  <action>按 D-05 和 D-06 实现 `cloudfunctions/quick-entry/index.ts`，接收固定 payload：`asset`、`rooms`、`tenant`、`lease`、`mode: 'quick-entry'`。整租模式强制通过 `createAssetWithDefaultRoomForWholeMode` 建档，分租模式允许同次提交房间集合但只创建一个当前 active lease。新增 `miniprogram/services/quick-entry.ts`，只暴露 `submitQuickEntry()`，内部唯一调用 `quick-entry` 云函数；明确禁止该 wrapper 再串联 `assets-save`、`rooms-save`、`tenants-save`、`leases-save`。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/quick-entry.spec.ts</automated>
  </verify>
  <done>前端和后端都拥有独立的快速录入 one-shot 通道，满足 D-06，不会退化成多次单实体保存。</done>
</task>

<task type="auto">
  <name>Task 2: 实现房源、房间、租户、租约分步维护与结束租约入口</name>
  <files>cloudfunctions/assets-save/index.ts, cloudfunctions/rooms-save/index.ts, cloudfunctions/tenants-save/index.ts, cloudfunctions/leases-save/index.ts, cloudfunctions/leases-end/index.ts, miniprogram/services/asset.ts, miniprogram/services/room.ts, miniprogram/services/tenant.ts, miniprogram/services/lease.ts, tests/cloud/entity-save.spec.ts</files>
  <action>按 D-07 提供单实体维护入口。`assets-save` 只处理房源增改；`rooms-save` 只处理房间增改并服务 `ASST-02`；`tenants-save` 只处理租户基础档案；`leases-save` 只处理租约增改；`leases-end` 只结束当前租约并返回新的状态摘要。客户端服务按实体拆分包装 `wx.cloud.callFunction`，页面不得直调 collection，也不得在任意 save 函数里偷偷维护 `currentTenant` 或手工状态字段。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/entity-save.spec.ts</automated>
  </verify>
  <done>写入侧已经形成两条清晰路径：快速录入 one-shot 和分步维护单实体保存，且房间编辑拥有独立后端入口。</done>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/quick-entry.spec.ts tests/cloud/entity-save.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
用户可以在不依赖导入的前提下完成基础经营数据建档，且快速录入与分步维护的边界被代码强制执行。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-03-SUMMARY.md`
</output>
