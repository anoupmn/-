---
phase: 01-核心数据骨架
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - cloudfunctions/quick-entry/index.ts
  - cloudfunctions/assets-save/index.ts
  - cloudfunctions/rooms-save/index.ts
  - cloudfunctions/tenants-save/index.ts
  - cloudfunctions/leases-save/index.ts
  - cloudfunctions/leases-end/index.ts
  - cloudfunctions/rentable-units-list/index.ts
  - cloudfunctions/rentable-unit-detail/index.ts
  - miniprogram/services/asset.ts
  - miniprogram/services/room.ts
  - miniprogram/services/tenant.ts
  - miniprogram/services/lease.ts
  - miniprogram/services/rentable-unit.ts
  - tests/cloud/quick-entry.spec.ts
  - tests/cloud/rentable-units-list.spec.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - LEASE-01
  - LEASE-02
  - LEASE-03
  - LEASE-04
  - LEASE-05
must_haves:
  truths:
    - "快速录入可以一次性写入房源/房间、当前租户和当前租约。"
    - "分步维护可以分别保存房源、房间、租户、租约，并支持结束租约。"
    - "列表查询与详情查询返回的当前租户、当前状态和历史租约都来自统一领域计算结果。"
  artifacts:
    - path: "cloudfunctions/quick-entry/index.ts"
      provides: "首次建档的一次性写入入口"
      contains: "mode: 'quick-entry'"
    - path: "cloudfunctions/rentable-units-list/index.ts"
      provides: "列表首屏摘要数据"
      contains: "nextReceivableAmount"
    - path: "cloudfunctions/rentable-unit-detail/index.ts"
      provides: "可出租单元详情与历任租约历史"
      contains: "leaseHistory"
  key_links:
    - from: "miniprogram/services/rentable-unit.ts"
      to: "cloudfunctions/rentable-units-list/index.ts"
      via: "wx.cloud.callFunction"
      pattern: "rentable-units-list"
    - from: "cloudfunctions/quick-entry/index.ts"
      to: "cloudfunctions/shared/repositories/lease-repository.ts"
      via: "createLease"
      pattern: "createLease"
    - from: "cloudfunctions/rentable-unit-detail/index.ts"
      to: "cloudfunctions/shared/calculators/rentable-unit.ts"
      via: "assemble detail and history"
      pattern: "leaseHistory"
---

<objective>
实现手动录入与查询所需的云函数入口和客户端服务包装。

Purpose: 把前一计划的领域契约变成可调用的保存、结束、列表和详情能力，为 Phase 1 页面层提供稳定 API。
Output: 快速录入云函数、分步维护云函数、列表/详情查询云函数与对应前端服务。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/phases/01-核心数据骨架/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现快速录入与分步维护云函数入口</name>
  <files>cloudfunctions/quick-entry/index.ts, cloudfunctions/assets-save/index.ts, cloudfunctions/rooms-save/index.ts, cloudfunctions/tenants-save/index.ts, cloudfunctions/leases-save/index.ts, cloudfunctions/leases-end/index.ts, miniprogram/services/asset.ts, miniprogram/services/room.ts, miniprogram/services/tenant.ts, miniprogram/services/lease.ts, tests/cloud/quick-entry.spec.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-核心数据骨架/1-CONTEXT.md, cloudfunctions/quick-entry/index.ts, cloudfunctions/shared/repositories/asset-repository.ts, cloudfunctions/shared/repositories/lease-repository.ts, tests/cloud/quick-entry.spec.ts</read_first>
  <acceptance_criteria>
    - `cloudfunctions/quick-entry/index.ts` contains `mode: 'quick-entry'`
    - `cloudfunctions/quick-entry/index.ts` contains `createAssetWithDefaultRoomForWholeMode`
    - `cloudfunctions/leases-end/index.ts` contains `endLease`
    - `miniprogram/services/lease.ts` contains `endLease`
    - `tests/cloud/quick-entry.spec.ts` contains `creates asset room tenant and active lease in one request`
  </acceptance_criteria>
  <action>按 D-05、D-06、D-07 把录入 API 分成两类。`cloudfunctions/quick-entry/index.ts` 接收固定 payload：`asset`, `rooms`, `tenant`, `lease`, `mode: 'quick-entry'`，在整租模式下强制调用 `createAssetWithDefaultRoomForWholeMode`，在分租模式下允许批量创建房间但首次只创建一个 active lease；`assets-save`、`rooms-save`、`tenants-save`、`leases-save` 作为分步维护入口，各自只处理单实体 upsert；`leases-end` 仅负责结束租约并返回新的 `currentStatus`。客户端 `miniprogram/services/*.ts` 统一封装 `wx.cloud.callFunction`，禁止页面直接传裸 collection 名称。先写 `tests/cloud/quick-entry.spec.ts`，至少覆盖快速录入成功、整租默认房间创建、重复 active lease 拒绝、结束租约后状态变化四个场景。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/quick-entry.spec.ts</automated>
  </verify>
  <done>录入能力已区分为快速录入与分步维护，且所有写入都经过统一领域规则，不会绕开 active lease 校验。</done>
</task>

<task type="auto">
  <name>Task 2: 实现列表与详情查询云函数以及前端读取服务</name>
  <files>cloudfunctions/rentable-units-list/index.ts, cloudfunctions/rentable-unit-detail/index.ts, miniprogram/services/rentable-unit.ts, tests/cloud/rentable-units-list.spec.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-核心数据骨架/1-CONTEXT.md, cloudfunctions/rentable-units-list/index.ts, cloudfunctions/rentable-unit-detail/index.ts, miniprogram/services/rentable-unit.ts</read_first>
  <action>实现 Phase 1 查询面。`rentable-units-list` 返回字段固定为 D-08 规定的 8 个首屏字段：`displayName`、`currentStatus`、`currentTenantName`、`nextReceivableDate`、`nextReceivableAmount`、`isAbnormal`、`vacancyDays`、`overdueDays`，其中 `vacancyDays` 与 `overdueDays` 二选一输出为 `statusMetricValue` 以供页面渲染；`rentable-unit-detail` 返回 `asset`、`room`、`activeLease`、`leaseHistory`、`tenantHistory`；`miniprogram/services/rentable-unit.ts` 暴露 `listRentableUnits` 与 `getRentableUnitDetail`。禁止在查询函数里新增“当前租户”持久化字段，必须从 active lease 拼装结果。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/rentable-units-list.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/rentable-units-list/index.ts` contains `nextReceivableDate`
    - `cloudfunctions/rentable-units-list/index.ts` contains `nextReceivableAmount`
    - `cloudfunctions/rentable-units-list/index.ts` contains `statusMetricValue`
    - `cloudfunctions/rentable-unit-detail/index.ts` contains `tenantHistory`
    - `miniprogram/services/rentable-unit.ts` contains `listRentableUnits`
    - `tests/cloud/rentable-units-list.spec.ts` contains `returns first-screen fields for each rentable unit`
  </acceptance_criteria>
  <done>页面层已有稳定查询接口，且列表首屏与历史详情都来自统一云函数输出。</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --runTestsByPath tests/cloud/quick-entry.spec.ts tests/cloud/rentable-units-list.spec.ts`
- [ ] `npm run typecheck`
- [ ] 所有客户端服务均通过 `wx.cloud.callFunction` 调用，页面不直接访问数据库
</verification>

<success_criteria>

- 所有任务完成
- 快速录入、分步维护、结束租约、列表查询、历史详情五类能力都有明确云函数入口
- 查询和写入都复用同一套领域契约
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-03-SUMMARY.md`
</output>
