---
phase: 01-核心数据骨架
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - cloudfunctions/rentable-units-list/index.ts
  - cloudfunctions/rentable-unit-detail/index.ts
  - miniprogram/services/rentable-unit.ts
  - tests/cloud/rentable-units-list.spec.ts
  - tests/cloud/rentable-unit-detail.spec.ts
autonomous: true
requirements:
  - LIST-01
  - LEASE-05
must_haves:
  truths:
    - "列表查询返回第 1 阶段首屏经营字段。"
    - "详情查询返回当前租约、租约历史和租户历史。"
    - "查询结果中的当前租户和状态来自领域计算，而不是额外持久化字段。"
  artifacts:
    - "cloudfunctions/rentable-units-list/index.ts: 列表摘要查询"
    - "cloudfunctions/rentable-unit-detail/index.ts: 详情与历史查询"
    - "miniprogram/services/rentable-unit.ts: 列表/详情客户端服务"
  key_links:
    - "miniprogram/services/rentable-unit.ts -> cloudfunctions/rentable-units-list/index.ts"
    - "miniprogram/services/rentable-unit.ts -> cloudfunctions/rentable-unit-detail/index.ts"
    - "rentable-unit-detail -> lease-lifecycle/rentable-unit calculators"
---

<objective>
实现经营列表与单元详情所需的查询云函数。

Purpose: 先把可读数据面稳定下来，页面层只负责展示，不再重复拼装当前租户、状态和历史关系。
Output: 列表查询云函数、详情查询云函数与对应前端服务。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/phases/01-核心数据骨架/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现经营列表查询云函数</name>
  <files>cloudfunctions/rentable-units-list/index.ts, miniprogram/services/rentable-unit.ts, tests/cloud/rentable-units-list.spec.ts</files>
  <behavior>
    - Test 1: 每个可出租单元返回 D-08 规定的首屏字段。
    - Test 2: 当前租户姓名来自 active lease，而不是独立字段。
    - Test 3: `vacancyDays` 或 `overdueDays` 会落到统一展示指标。
  </behavior>
  <action>实现 `rentable-units-list`，返回字段固定为 `displayName`、`currentStatus`、`currentTenantName`、`nextReceivableDate`、`nextReceivableAmount`、`isAbnormal`、`vacancyDays`、`overdueDays`、`statusMetricValue`。`miniprogram/services/rentable-unit.ts` 暴露 `listRentableUnits()`。禁止在查询阶段新增任何 `currentTenant` 持久化字段。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/rentable-units-list.spec.ts</automated>
  </verify>
  <done>列表页所需首屏经营字段已经由云函数统一提供。</done>
</task>

<task type="auto">
  <name>Task 2: 实现单元详情与历史查询云函数</name>
  <files>cloudfunctions/rentable-unit-detail/index.ts, miniprogram/services/rentable-unit.ts, tests/cloud/rentable-unit-detail.spec.ts</files>
  <action>实现 `rentable-unit-detail`，返回 `asset`、`room`、`activeLease`、`leaseHistory`、`tenantHistory`。详情查询只负责组装当前实体与历史，不拉入 Phase 2 的分项账单明细。客户端服务新增 `getRentableUnitDetail()`，供后续详情页直接消费。</action>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/rentable-unit-detail.spec.ts</automated>
  </verify>
  <done>详情页已有稳定的当前租约和历史查询接口，足以支撑 LEASE-05 与 LIST-01 的页面展示。</done>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/rentable-units-list.spec.ts tests/cloud/rentable-unit-detail.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
页面层已经拥有稳定的列表与详情读取服务，不需要自己重新计算经营字段或租户历史。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-04-SUMMARY.md`
</output>
