---
phase: 01-核心数据骨架
plan: 06
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-03
  - 01-04
files_modified:
  - miniprogram/app.json
  - miniprogram/pages/units/index.json
  - miniprogram/pages/units/index.ts
  - miniprogram/pages/units/index.wxml
  - miniprogram/pages/units/index.wxss
  - miniprogram/pages/unit-detail/index.json
  - miniprogram/pages/unit-detail/index.ts
  - miniprogram/pages/unit-detail/index.wxml
  - miniprogram/pages/unit-detail/index.wxss
autonomous: true
requirements:
  - LIST-01
  - LEASE-04
  - LEASE-05
must_haves:
  truths:
    - "用户可以浏览所有房屋和房间的列表，并看到首屏经营字段。"
    - "用户可以进入单元详情查看当前租约、租约历史和租户历史。"
    - "用户可以从详情页结束租约并返回列表刷新结果。"
  artifacts:
    - "miniprogram/pages/units/*: 经营列表页"
    - "miniprogram/pages/unit-detail/*: 单元详情与历史页"
    - "miniprogram/app.json: 页面路由注册"
  key_links:
    - "units page -> miniprogram/services/rentable-unit.ts via listRentableUnits"
    - "unit-detail page -> miniprogram/services/rentable-unit.ts via getRentableUnitDetail"
    - "unit-detail page -> miniprogram/services/lease.ts via endLease"
---

<objective>
交付经营列表页与单元详情历史页。

Purpose: 让 Phase 1 的数据骨架在 UI 上可浏览、可核对、可结束租约。
Output: 房屋/房间列表页、可出租单元详情页，以及相关页面路由。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-核心数据骨架/1-CONTEXT.md
@.planning/phases/01-核心数据骨架/01-03-PLAN.md
@.planning/phases/01-核心数据骨架/01-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现经营列表页</name>
  <files>miniprogram/pages/units/index.json, miniprogram/pages/units/index.ts, miniprogram/pages/units/index.wxml, miniprogram/pages/units/index.wxss, miniprogram/app.json</files>
  <action>注册 `pages/units/index` 并在 `onShow` 调用 `listRentableUnits()`。每条记录第一屏固定展示 D-08 字段：名称、当前状态、当前租户姓名、下一次应收日期、下一次应收金额、是否异常、空置时长或逾期天数。列表视觉使用卡片而不是表格，适配微信小程序窄屏；页面不得自行计算当前租户或状态。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>列表页可以稳定展示 Phase 1 首屏经营信息，并成为认证后的默认浏览入口。</done>
</task>

<task type="auto">
  <name>Task 2: 实现单元详情页与结束租约交互</name>
  <files>miniprogram/pages/unit-detail/index.json, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/pages/unit-detail/index.wxss, miniprogram/app.json</files>
  <action>注册 `pages/unit-detail/index` 并在 `onLoad` 调用 `getRentableUnitDetail()`，顶部显示当前租约摘要，下方显示 `leaseHistory` 与 `tenantHistory`。页面提供 `结束租约` 按钮并调用 `endLease()`；完成后返回列表页并刷新。详情页保持在 Phase 1 范围内，不加入分项账单和账单状态块。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>用户已经能查看当前租约与历史关系，并在详情页完成租约结束操作。</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- 页面路由包含 `units` 与 `unit-detail`
</verification>

<success_criteria>
LIST-01、LEASE-04、LEASE-05 已在前端可见且可操作，Phase 1 的数据骨架具备真实日常浏览价值。
</success_criteria>

<output>
After completion, create `.planning/phases/01-核心数据骨架/01-06-SUMMARY.md`
</output>
