---
phase: 02-账单与房态视图
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudfunctions/shared/constants/collections.ts
  - cloudfunctions/shared/constants/statuses.ts
  - cloudfunctions/shared/schemas/lease.ts
  - cloudfunctions/shared/schemas/bill.ts
  - cloudfunctions/shared/calculators/lease-lifecycle.ts
  - cloudfunctions/shared/calculators/bill-status.ts
  - cloudfunctions/shared/calculators/rentable-unit.ts
  - cloudfunctions/shared/repositories/lease-repository.ts
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/leases-save/index.ts
  - tests/domain/bill-status.spec.ts
  - tests/domain/rentable-unit-status.spec.ts
  - tests/cloud/bills-sync.spec.ts
  - tests/cloud/leases-save-billing.spec.ts
  - tests/helpers/mock-cloud.ts
autonomous: true
requirements:
  - BILL-01
  - BILL-03
must_haves:
  truths:
    - "每个租约都可以表达固定大类费用和追加费用项，不再只有 rentAmount / depositAmount 两个账单事实。"
    - "系统能基于租约费用规则生成具体 bill 记录，并从 bill 记录得出下一笔应收日期和金额。"
    - "账单状态计算由共享领域代码统一负责，而不是分散在云函数或页面里。"
  artifacts:
    - "cloudfunctions/shared/schemas/bill.ts: bill 实体与费用规则 schema"
    - "cloudfunctions/shared/calculators/bill-status.ts: 账单状态与风险窗口计算"
    - "cloudfunctions/shared/repositories/bill-repository.ts: bill 生成与同步入口"
    - "tests/cloud/bills-sync.spec.ts: 租约到 bill 生成覆盖"
  key_links:
    - "lease.ts -> bill.ts via 费用规则字段"
    - "lease-repository.ts -> bill-repository.ts via save 后同步账单"
    - "bill-status.ts -> rentable-unit.ts via 下一笔应收与风险标签"
---

<objective>
建立 Phase 2 的账单领域骨架，让租约费用规则、bill 记录和共享状态计算器形成稳定底座。

Purpose: 如果账单模型和 bill 同步逻辑不先锁住，后续详情页、收款动作和列表房态都会建立在会漂移的数据上。
Output: `bills` 集合定义、费用规则 schema、bill 状态计算器、bill 仓储，以及租约保存时的账单同步能力。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-账单与房态视图/2-CONTEXT.md
@.planning/phases/02-账单与房态视图/02-RESEARCH.md
@cloudfunctions/shared/calculators/lease-lifecycle.ts
@cloudfunctions/shared/calculators/rentable-unit.ts
@cloudfunctions/shared/schemas/lease.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用测试锁定 bill schema、费用规则和状态计算</name>
  <files>cloudfunctions/shared/constants/collections.ts, cloudfunctions/shared/constants/statuses.ts, cloudfunctions/shared/schemas/lease.ts, cloudfunctions/shared/schemas/bill.ts, cloudfunctions/shared/calculators/lease-lifecycle.ts, cloudfunctions/shared/calculators/bill-status.ts, cloudfunctions/shared/calculators/rentable-unit.ts, tests/domain/bill-status.spec.ts, tests/domain/rentable-unit-status.spec.ts, tests/helpers/mock-cloud.ts</files>
  <read_first>.planning/phases/02-账单与房态视图/2-CONTEXT.md, .planning/phases/02-账单与房态视图/02-RESEARCH.md, cloudfunctions/shared/constants/statuses.ts, cloudfunctions/shared/calculators/rentable-unit.ts</read_first>
  <action>先补失败测试，再补实现。新增 `bills` collection，并在 `statuses.ts` 中明确账单状态枚举、主状态和风险标签映射。扩展 `lease.ts`，把费用规则表达成“固定大类 + custom items”的可验证 schema，同时保留对 Phase 1 `rentAmount` / `depositAmount` 调用路径的兼容迁移位。新增 `bill.ts` schema，至少包含 `leaseId`、`roomId`、`type`、`section`、`dueDate`、`amount`、`status`、`receivedAt`、`receivedAmount`。`bill-status.ts` 必须提供账单状态推导、15 天内到期识别、押金不逾期规则。更新 `rentable-unit.ts`，让它输出主状态、风险标签、下一笔应收摘要所需的衍生字段，但仍禁止持久化 `currentStatus` 真相字段。</action>
  <verify>
    <automated>npm test -- --runInBand --runTestsByPath tests/domain/bill-status.spec.ts tests/domain/rentable-unit-status.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/shared/constants/collections.ts` contains `bills`
    - `cloudfunctions/shared/schemas/bill.ts` contains `receivedAt`
    - `cloudfunctions/shared/schemas/lease.ts` contains `customFeeItems`
    - `cloudfunctions/shared/calculators/bill-status.ts` contains `due_today`
    - `cloudfunctions/shared/calculators/bill-status.ts` contains `overdue`
    - `tests/domain/bill-status.spec.ts` contains `deposit`
    - `tests/domain/rentable-unit-status.spec.ts` contains `expiring`
  </acceptance_criteria>
  <done>账单实体、费用规则和共享状态推导已经有自动化测试兜底，后续云函数可以直接复用这些领域契约。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现租约保存时的 bill 同步与下一笔应收推导</name>
  <files>cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/leases-save/index.ts, tests/cloud/bills-sync.spec.ts, tests/cloud/leases-save-billing.spec.ts, tests/helpers/mock-cloud.ts</files>
  <read_first>.planning/phases/01-核心数据骨架/01-03-PLAN.md, .planning/phases/02-账单与房态视图/2-CONTEXT.md, cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/runtime.ts, tests/cloud/entity-save.spec.ts</read_first>
  <action>新增 `bill-repository.ts`，提供至少 `syncBillsForLease()`、`listBillsByLease()`、`listOutstandingBillsByRoom()` 这些入口。同步逻辑要把租约费用规则转成具体 bill 记录：房租按 `billingCycleDays` 周期生成当前有效期内的应收，押金按一次性 bill 生成，其余固定/追加费用按约定 cadence 生成；Phase 2 不做部分收款。更新 `lease-repository.ts` 和 `cloudfunctions/leases-save/index.ts`，在创建或更新租约后同步 bill，确保后续列表/详情总能拿到下一笔应收事实。测试要覆盖：首次建租约生成 bill、更新费用规则后重算 bill、押金不逾期、下一笔应收来自 bill 而不是旧的 `rentAmount` 单字段。</action>
  <verify>
    <automated>npm test -- --runInBand --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/shared/repositories/bill-repository.ts` contains `syncBillsForLease`
    - `cloudfunctions/shared/repositories/bill-repository.ts` contains `listBillsByLease`
    - `cloudfunctions/shared/repositories/lease-repository.ts` contains `syncBillsForLease`
    - `cloudfunctions/leases-save/index.ts` contains `createLease`
    - `tests/cloud/bills-sync.spec.ts` contains `receivedAt`
    - `tests/cloud/leases-save-billing.spec.ts` contains `next receivable`
  </acceptance_criteria>
  <done>租约保存已经会同步具体账单记录，Phase 2 后续所有查询和 UI 都能建立在 bill 事实之上。</done>
</task>

</tasks>

<verification>
- `npm test -- --runInBand --runTestsByPath tests/domain/bill-status.spec.ts tests/domain/rentable-unit-status.spec.ts`
- `npm test -- --runInBand --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
Phase 2 的账单数据骨架已经稳定：租约可表达分项费用，bill 记录可被生成，下一笔应收可以由共享领域代码可靠推导。
</success_criteria>

<output>
After completion, create `.planning/phases/02-账单与房态视图/02-01-SUMMARY.md`
</output>
