---
phase: 02-账单与房态视图
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/shared/calculators/rentable-unit.ts
  - cloudfunctions/rentable-units-list/index.ts
  - cloudfunctions/rentable-unit-detail/index.ts
  - cloudfunctions/bills-receive/index.ts
  - cloudfunctions/bills-receive/package.json
  - miniprogram/services/bill.ts
  - miniprogram/services/rentable-unit.ts
  - miniprogram/services/lease.ts
  - tests/cloud/bills-receive.spec.ts
  - tests/cloud/rentable-unit-detail-billing.spec.ts
  - tests/cloud/rentable-units-list-status.spec.ts
  - miniprogram/app.json
autonomous: true
requirements:
  - ASST-04
  - BILL-02
  - BILL-03
  - LIST-02
must_haves:
  truths:
    - "列表和详情查询都能返回主状态、风险标签、当前租约应收摘要，不再只返回 Phase 1 的单枚举状态。"
    - "详情查询能按 `房租 / 押金 / 非房租类费用` 返回当前账单分区。"
    - "登记收款会更新 bill 的 `receivedAt` 和 `receivedAmount`，从而改变后续状态和摘要。"
  artifacts:
    - "cloudfunctions/bills-receive/index.ts: 收款登记入口"
    - "cloudfunctions/rentable-unit-detail/index.ts: 含摘要卡与费用分区的详情查询"
    - "cloudfunctions/rentable-units-list/index.ts: 含主状态与风险标签的列表查询"
    - "miniprogram/services/bill.ts: 收款动作 service"
  key_links:
    - "bills-receive/index.ts -> bill-repository.ts via markBillReceived"
    - "rentable-unit-detail/index.ts -> bill-repository.ts via fee section aggregation"
    - "rentable-units-list/index.ts -> rentable-unit.ts via mainStatus / riskTags"
---

<objective>
把 bill 事实接到查询与动作层，让后端可以完整提供列表房态、详情摘要、费用分区和收款登记能力。

Purpose: 没有查询聚合和收款动作，前端只能看到静态账单结构，无法支撑 LIST-02、BILL-02 和 ASST-04。
Output: bills-receive 云函数、增强后的 rentable-units-list / rentable-unit-detail 查询、对应 miniprogram service。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-账单与房态视图/2-CONTEXT.md
@.planning/phases/02-账单与房态视图/02-RESEARCH.md
@.planning/phases/02-账单与房态视图/02-UI-SPEC.md
@.planning/phases/02-账单与房态视图/02-01-PLAN.md
@cloudfunctions/rentable-unit-detail/index.ts
@cloudfunctions/rentable-units-list/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现收款登记与详情账单分区聚合</name>
  <files>cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/rentable-unit-detail/index.ts, cloudfunctions/bills-receive/index.ts, cloudfunctions/bills-receive/package.json, miniprogram/services/bill.ts, tests/cloud/bills-receive.spec.ts, tests/cloud/rentable-unit-detail-billing.spec.ts</files>
  <read_first>.planning/phases/02-账单与房态视图/2-CONTEXT.md, .planning/phases/02-账单与房态视图/02-UI-SPEC.md, cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/rentable-unit-detail/index.ts</read_first>
  <action>新增 `bills-receive` 云函数和 `miniprogram/services/bill.ts`，提供“登记收款”动作，payload 至少包含 `billId`、`receivedAt`、`receivedAmount`。在仓储层实现 `markBillReceived()`，禁止引入部分收款分支；收款后 bill 状态必须变为已收。增强 `rentable-unit-detail`，除了 `asset`、`room`、`activeLease`、`leaseHistory`、`tenantHistory`，还要返回 `summaryCard`、`primaryActions`、`feeSections`、`historyCollapsedByDefault` 这些直接服务 UI-SPEC 的字段。`feeSections` 必须按 `房租 / 押金 / 非房租类费用` 分区，并在每条 bill 上附带状态、应收日期、金额、收款日期。</action>
  <verify>
    <automated>npm test -- --runInBand --runTestsByPath tests/cloud/bills-receive.spec.ts tests/cloud/rentable-unit-detail-billing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/bills-receive/index.ts` contains `receivedAmount`
    - `cloudfunctions/shared/repositories/bill-repository.ts` contains `markBillReceived`
    - `cloudfunctions/rentable-unit-detail/index.ts` contains `feeSections`
    - `cloudfunctions/rentable-unit-detail/index.ts` contains `summaryCard`
    - `miniprogram/services/bill.ts` contains `receiveBill`
    - `tests/cloud/bills-receive.spec.ts` contains `paid`
    - `tests/cloud/rentable-unit-detail-billing.spec.ts` contains `feeSections`
  </acceptance_criteria>
  <done>后端已经能登记收款，并返回详情页渲染当前账单分区和摘要卡所需的完整数据。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 升级列表房态摘要为主状态 + 风险标签</name>
  <files>cloudfunctions/shared/calculators/rentable-unit.ts, cloudfunctions/rentable-units-list/index.ts, miniprogram/services/rentable-unit.ts, miniprogram/services/lease.ts, tests/cloud/rentable-units-list-status.spec.ts</files>
  <read_first>.planning/phases/02-账单与房态视图/2-CONTEXT.md, .planning/phases/02-账单与房态视图/02-UI-SPEC.md, cloudfunctions/shared/calculators/rentable-unit.ts, cloudfunctions/rentable-units-list/index.ts</read_first>
  <action>重构 `rentable-unit.ts` 和 `rentable-units-list` 输出，让当前房态不再只是一条 `currentStatus`。新增 `mainStatus`、`mainStatusLabel`、`riskTags`、`riskTagLabels`、`summaryHint` 等列表字段，并确保“15 天内到期”与“已逾期 + 异常”能并存展示。保留必要的兼容字段仅作为迁移过渡，但页面后续应转向新字段。列表中 `逾期天数` 与 `空置天数` 只在适用时返回，不要默认返回 0 占位。必要时在 service 层补充新的 receipt/detail 包装，但不要让页面自行推导风险标签。</action>
  <verify>
    <automated>npm test -- --runInBand --runTestsByPath tests/cloud/rentable-units-list-status.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cloudfunctions/shared/calculators/rentable-unit.ts` contains `mainStatus`
    - `cloudfunctions/shared/calculators/rentable-unit.ts` contains `riskTags`
    - `cloudfunctions/rentable-units-list/index.ts` contains `riskTagLabels`
    - `cloudfunctions/rentable-units-list/index.ts` contains `summaryHint`
    - `tests/cloud/rentable-units-list-status.spec.ts` contains `15`
    - `tests/cloud/rentable-units-list-status.spec.ts` contains `abnormal`
  </acceptance_criteria>
  <done>列表查询已经能提供“主状态 + 风险标签”的完整房态摘要，前端无需再把单枚举状态硬解释成多个经营风险。</done>
</task>

</tasks>

<verification>
- `npm test -- --runInBand --runTestsByPath tests/cloud/bills-receive.spec.ts tests/cloud/rentable-unit-detail-billing.spec.ts`
- `npm test -- --runInBand --runTestsByPath tests/cloud/rentable-units-list-status.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
后端查询和动作层已经能支撑 Phase 2 的房态、账单状态和详情展示需求，前端只需消费聚合结果。
</success_criteria>

<output>
After completion, create `.planning/phases/02-账单与房态视图/02-02-SUMMARY.md`
</output>
