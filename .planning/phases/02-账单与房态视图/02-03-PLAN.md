---
phase: 02-账单与房态视图
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - miniprogram/app.json
  - miniprogram/pages/units/index.ts
  - miniprogram/pages/units/index.wxml
  - miniprogram/pages/units/index.wxss
  - miniprogram/pages/unit-detail/index.json
  - miniprogram/pages/unit-detail/index.ts
  - miniprogram/pages/unit-detail/index.wxml
  - miniprogram/pages/unit-detail/index.wxss
  - miniprogram/services/bill.ts
  - miniprogram/services/rentable-unit.ts
autonomous: true
requirements:
  - ASST-04
  - BILL-02
  - BILL-04
  - LIST-02
must_haves:
  truths:
    - "房屋列表第一屏能快速看见主状态、风险标签、当前租户、下一笔应收，不再显示一排默认 0 值。"
    - "单元详情第一屏是经营摘要卡和高频动作，不会把历史区放在前面。"
    - "详情页能展示房租、押金、非房租类费用三段账单，并允许登记收款。"
  artifacts:
    - "miniprogram/pages/units/*: Phase 2 列表信息层级"
    - "miniprogram/pages/unit-detail/*: 摘要卡、账单分区、历史折叠与收款动作"
    - "miniprogram/services/bill.ts: 收款调用封装"
  key_links:
    - "unit-detail page -> receiveBill -> bills-receive 云函数"
    - "unit-detail page -> getRentableUnitDetail -> feeSections / summaryCard"
    - "units page -> listRentableUnits -> mainStatus / riskTags"
---

<objective>
把 Phase 2 的账单与房态能力真正接到小程序页面，完成列表与详情的经营视图升级。

Purpose: 如果 UI 仍停留在 Phase 1 结构，后端账单和房态能力就无法转化成日常可用的经营界面。
Output: 升级后的 `units` 与 `unit-detail` 页面、收款交互、历史默认折叠、状态标签和费用分区展示。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-账单与房态视图/2-CONTEXT.md
@.planning/phases/02-账单与房态视图/02-UI-SPEC.md
@.planning/phases/02-账单与房态视图/02-02-PLAN.md
@miniprogram/pages/units/index.ts
@miniprogram/pages/unit-detail/index.ts
@miniprogram/app.wxss
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级房屋列表为主状态 + 风险标签摘要卡</name>
  <files>miniprogram/pages/units/index.ts, miniprogram/pages/units/index.wxml, miniprogram/pages/units/index.wxss, miniprogram/services/rentable-unit.ts</files>
  <read_first>.planning/phases/02-账单与房态视图/02-UI-SPEC.md, .planning/phases/02-账单与房态视图/2-CONTEXT.md, miniprogram/pages/units/index.ts, miniprogram/pages/units/index.wxml</read_first>
  <action>调整 `pages/units/index`，消费新的 `mainStatusLabel`、`riskTagLabels`、`summaryHint`、`nextReceivableDate`、`nextReceivableAmount` 字段。列表卡片必须把主状态和风险标签放到上部，次级提示只在有值时显示，不允许继续渲染默认 `0` 的逾期/空置字段。样式保持项目现有黑白灰基调，但按 UI-SPEC 建立更清晰的信息层次；风险标签必须带文字，不得只靠颜色。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `miniprogram/pages/units/index.wxml` contains `mainStatusLabel`
    - `miniprogram/pages/units/index.wxml` contains `riskTagLabels`
    - `miniprogram/pages/units/index.wxml` does not contain `逾期天数：{{item.overdueDays || 0}}`
    - `miniprogram/pages/units/index.wxml` does not contain `空置天数：{{item.vacancyDays || 0}}`
    - `miniprogram/pages/units/index.ts` contains `listRentableUnits`
  </acceptance_criteria>
  <done>列表页已经变成真正可扫读的经营入口，能一眼看出状态、风险和下一笔应收。</done>
</task>

<task type="auto">
  <name>Task 2: 重构单元详情为摘要卡 + 费用分区 + 收款动作</name>
  <files>miniprogram/app.json, miniprogram/pages/unit-detail/index.json, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/pages/unit-detail/index.wxss, miniprogram/services/bill.ts, miniprogram/services/rentable-unit.ts</files>
  <read_first>.planning/phases/02-账单与房态视图/02-UI-SPEC.md, .planning/phases/02-账单与房态视图/2-CONTEXT.md, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/app.wxss</read_first>
  <action>重构 `unit-detail` 页面：顶部先渲染经营摘要卡，再渲染高频动作区、当前租约区、费用分区、历史区。费用区必须按 `房租`、`押金`、`非房租类费用` 分块展示，并对已收记录显示收款日期。接入 `receiveBill()` 实现“登记收款”动作，成功后刷新详情，不允许把收款写成租约字段编辑。历史租约和历任租户默认折叠，可通过文字按钮展开；`结束租约` 作为 destructive 动作必须与主收款动作明显区分。</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `miniprogram/pages/unit-detail/index.wxml` contains `summaryCard`
    - `miniprogram/pages/unit-detail/index.wxml` contains `登记收款`
    - `miniprogram/pages/unit-detail/index.wxml` contains `查看全部账单`
    - `miniprogram/pages/unit-detail/index.wxml` contains `非房租类费用`
    - `miniprogram/pages/unit-detail/index.wxml` contains `历史租约`
    - `miniprogram/pages/unit-detail/index.ts` contains `receiveBill`
    - `miniprogram/pages/unit-detail/index.ts` contains `historyCollapsed`
    - `miniprogram/services/bill.ts` contains `receiveBill`
  </acceptance_criteria>
  <done>详情页已经遵守 Phase 2 的 UI 合同，当前经营摘要、账单分区和收款动作都可直接服务日常收租场景。</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- 页面列表与详情均不再依赖 Phase 1 的单枚举状态文案
</verification>

<success_criteria>
用户进入列表和详情后，能够直接看到 Phase 2 需要的房态、风险和账单分区，并完成收款登记而不需要回退到租约编辑心智。
</success_criteria>

<output>
After completion, create `.planning/phases/02-账单与房态视图/02-03-SUMMARY.md`
</output>
