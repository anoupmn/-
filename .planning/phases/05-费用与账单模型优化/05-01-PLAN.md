---
phase: "05-费用与账单模型优化"
plan: "05-01"
title: "数据集合职责、操作写入边界与账单同步安全治理"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/mock-cloud.ts
  - tests/cloud/bills-sync.spec.ts
  - tests/cloud/leases-save-billing.spec.ts
  - tests/cloud/dashboard-home.spec.ts
  - tests/cloud/alerts-list.spec.ts
  - cloudfunctions/shared/runtime.ts
  - cloudfunctions/shared/runtime.js
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/shared/repositories/bill-repository.js
  - cloudfunctions/shared/repositories/alert-repository.ts
  - cloudfunctions/shared/repositories/alert-repository.js
  - cloudfunctions/rentable-unit-detail/index.ts
  - cloudfunctions/rentable-units-list/index.ts
requirements: [CORR-01]
requirements_addressed: [CORR-01]
autonomous: true
must_haves:
  truths:
    - "编辑租约只替换未收款的系统生成账单"
    - "已收账单和手工补录账单在租约保存后仍然存在"
    - "打开首页或提醒列表只重建当前房东的 alerts"
    - "业务记录查询和更新不依赖数据库 _id 等于业务 id"
  artifacts:
    - path: "cloudfunctions/shared/repositories/bill-repository.ts"
      provides: "安全的 syncBillsForLease 替换策略"
    - path: "cloudfunctions/shared/repositories/alert-repository.ts"
      provides: "按 landlordOpenId scoped 重建 alerts"
    - path: "cloudfunctions/shared/runtime.ts"
      provides: "业务 id 查询/更新工具"
    - path: "tests/cloud/bills-sync.spec.ts"
      provides: "账单同步保护性测试"
  key_links:
    - from: "cloudfunctions/shared/repositories/lease-repository.ts"
      to: "cloudfunctions/shared/repositories/bill-repository.ts"
      via: "updateLease 调用 syncBillsForLease"
      pattern: "syncBillsForLease\\(db, updatedLease"
    - from: "cloudfunctions/shared/repositories/alert-repository.ts"
      to: "alerts collection"
      via: "where({ landlordOpenId }).remove()"
      pattern: "where\\(\\{ landlordOpenId \\}\\)\\.remove"
---

<objective>
建立 Phase 05 的数据安全底座：先用测试锁定写入边界，再修正业务 `id`/`_id` 风险、`alerts` 全局清空风险，以及 `syncBillsForLease` 整批删除账单风险。

Purpose: 满足 D-01、D-04、D-05、D-06、D-07、D-08、D-09、D-10，并覆盖 `CORR-01`。
Output: 可被后续费用、水电、房东支出和删除策略复用的安全 repository/runtime 行为。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DATA-COLLECTION-MATRIX.md
@.planning/phases/05-费用与账单模型优化/5-CONTEXT.md
@.planning/phases/05-费用与账单模型优化/05-RESEARCH.md
@.planning/phases/05-费用与账单模型优化/05-VALIDATION.md
@cloudfunctions/shared/runtime.ts
@cloudfunctions/shared/repositories/bill-repository.ts
@cloudfunctions/shared/repositories/alert-repository.ts
@cloudfunctions/rentable-unit-detail/index.ts
@cloudfunctions/rentable-units-list/index.ts
@tests/helpers/mock-cloud.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>05-01-01</id>
  <title>补齐写入边界 Wave 0 测试</title>
  <name>Task 1: 补齐写入边界 Wave 0 测试</name>
  <files>tests/helpers/mock-cloud.ts, tests/cloud/bills-sync.spec.ts, tests/cloud/leases-save-billing.spec.ts, tests/cloud/dashboard-home.spec.ts, tests/cloud/alerts-list.spec.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/phases/05-费用与账单模型优化/05-VALIDATION.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - tests/helpers/mock-cloud.ts
    - tests/cloud/bills-sync.spec.ts
    - tests/cloud/leases-save-billing.spec.ts
    - tests/cloud/dashboard-home.spec.ts
    - tests/cloud/alerts-list.spec.ts
  </read_first>
  <action>
    在现有测试文件中新增明确用例，不创建平行测试框架：
    - `tests/cloud/bills-sync.spec.ts`：新增用例名称包含 `preserves paid bills and manual bills when lease is resynced`，构造同一 `leaseId` 下 `source: 'system'` 已收账单、`source: 'manual'` 手工账单和未收系统账单；期望重算后已收账单和手工账单仍存在，旧未收系统账单被替换。
    - `tests/cloud/leases-save-billing.spec.ts`：新增用例名称包含 `updates lease without silently deleting received or manual bills`，从 `leases-save` 入口验证 `CORR-01`，不要只测 repository。
    - `tests/cloud/dashboard-home.spec.ts` 与 `tests/cloud/alerts-list.spec.ts`：新增用例名称包含 `does not remove alerts from another landlord`，在 mock store 预置另一个 `landlordOpenId` 的 alert，调用当前房东入口后断言仍保留。
    - `tests/helpers/mock-cloud.ts`：让 mock 支持 `_id !== id` 的记录场景；`doc(id)` 只能命中 `_id`，`where({ id })` 才命中业务 id，用来暴露 D-06 风险。
  </action>
  <acceptance_criteria>
    - `rg -n "preserves paid bills and manual bills when lease is resynced" tests/cloud/bills-sync.spec.ts` 能找到测试名。
    - `rg -n "updates lease without silently deleting received or manual bills" tests/cloud/leases-save-billing.spec.ts` 能找到测试名。
    - `rg -n "does not remove alerts from another landlord" tests/cloud/dashboard-home.spec.ts tests/cloud/alerts-list.spec.ts` 至少返回 2 处。
    - `rg -n "_id" tests/helpers/mock-cloud.ts` 能看到 mock 记录支持 `_id` 字段。
  </acceptance_criteria>
  <done>写入边界测试名称和 mock `_id !== id` 能力均可通过 grep 验证。</done>
  <verify>
    <automated>rg -n "preserves paid bills and manual bills when lease is resynced|updates lease without silently deleting received or manual bills|does not remove alerts from another landlord|_id" tests/helpers/mock-cloud.ts tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/cloud/dashboard-home.spec.ts tests/cloud/alerts-list.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>05-01-02</id>
  <title>修正业务 id 查询更新、alerts scoped 重建与读侧只读边界</title>
  <name>Task 2: 修正业务 id 查询更新、alerts scoped 重建与读侧只读边界</name>
  <files>cloudfunctions/shared/runtime.ts, cloudfunctions/shared/runtime.js, cloudfunctions/shared/repositories/alert-repository.ts, cloudfunctions/shared/repositories/alert-repository.js, cloudfunctions/rentable-unit-detail/index.ts, cloudfunctions/rentable-units-list/index.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/runtime.ts
    - cloudfunctions/shared/repositories/alert-repository.ts
    - cloudfunctions/rentable-unit-detail/index.ts
    - cloudfunctions/rentable-units-list/index.ts
    - tests/helpers/mock-cloud.ts
    - tests/cloud/dashboard-home.spec.ts
    - tests/cloud/alerts-list.spec.ts
  </read_first>
  <action>
    按 D-04、D-06 修改共享运行时和提醒 repository：
    - 在 `cloudfunctions/shared/runtime.ts` 中把 `findById` 改为先 `where({ id }).get()` 查询业务 id；`updateRecord` 改为 `where({ id }).update({ data })` 后再通过业务 id 读取更新结果。
    - 保留 `doc()` 能力但不要让业务 repository 依赖 `doc(id)` 更新真相记录。
    - 新增或复用 scoped 删除工具，例如 `removeRecordsByQuery(db, collectionName, query)`，避免调用全局 `clearCollection`。
    - 在 `cloudfunctions/shared/repositories/alert-repository.ts` 中将 `rebuildAlerts` 改为只删除 `input.landlordOpenId` 对应 alerts；写入新 alert 时保持同一 `landlordOpenId`。
    - 按 D-05 审计 `cloudfunctions/rentable-unit-detail/index.ts` 与 `cloudfunctions/rentable-units-list/index.ts`：它们是展示型读侧函数，只能读取已有账单和聚合展示数据，不得补写、重建或同步账单。
    - 如果上述读侧函数存在 `syncBillsForLease`、`createBill`、`saveBill` 调用，或对 `bills` 集合执行 `.add(` / `.remove(`，删除这些写入副作用；保留或改为只读查询现有账单。
    - 同步维护 `.js` 对应文件；不要修改 `index 2.*`、`package 2.json` 旧拷贝文件。
  </action>
  <acceptance_criteria>
    - `rg -n "where\\(\\{ id \\}\\).*get|where\\(\\{ id \\}\\).*update" cloudfunctions/shared/runtime.ts` 能找到业务 id 查询/更新。
    - `rg -n "clearCollection\\(db, COLLECTIONS.alerts\\)" cloudfunctions/shared/repositories/alert-repository.ts` 没有结果。
    - `rg -n "where\\(\\{ landlordOpenId \\}\\)\\.remove" cloudfunctions/shared/repositories/alert-repository.ts` 能找到 scoped 删除。
    - `rg -n "syncBillsForLease|createBill|saveBill|\\.add\\(|\\.remove\\(" cloudfunctions/rentable-unit-detail/index.ts cloudfunctions/rentable-units-list/index.ts` 没有结果，证明 D-05 读侧无账单写入/同步副作用。
    - 当前房东重建 alerts 后，其他房东 alert 保留。
  </acceptance_criteria>
  <done>业务 id 查询更新通过 mock 测试，alerts 重建不删除其他房东数据，展示型读侧函数不写入或重建账单。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/dashboard-home.spec.ts tests/cloud/alerts-list.spec.ts --runInBand && ! rg -n "syncBillsForLease|createBill|saveBill|\\.add\\(|\\.remove\\(" cloudfunctions/rentable-unit-detail/index.ts cloudfunctions/rentable-units-list/index.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>05-01-03</id>
  <title>改造 syncBillsForLease 为安全替换</title>
  <name>Task 3: 改造 syncBillsForLease 为安全替换</name>
  <files>cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/shared/repositories/bill-repository.js</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/repositories/bill-repository.ts
    - cloudfunctions/shared/schemas/bill.ts
    - tests/cloud/bills-sync.spec.ts
    - tests/cloud/leases-save-billing.spec.ts
  </read_first>
  <action>
    按 D-07、D-08、D-09、D-10 实现账单同步保护：
    - 新增 `isReplaceableSystemBill(bill)`，条件为 `(bill.source ?? 'system') === 'system'`、`!bill.receivedAt`、`bill.receivedAmount === null`，且无 `receiptId`/`voidedAt`。
    - `syncBillsForLease` 先读取该租约现有账单，只删除满足 `isReplaceableSystemBill` 的旧系统账单；严禁出现 `where({ leaseId: lease.id }).remove()` 这种整批删除。
    - 已收账单、手工账单、未来含 `receiptId` 的账单全部保留。
    - 插入新系统账单时继续使用 `source: 'system'`，状态仍由 `deriveBillStatus` 计算。
    - 同步维护 `.js` 对应文件。
  </action>
  <acceptance_criteria>
    - `rg -n "isReplaceableSystemBill" cloudfunctions/shared/repositories/bill-repository.ts` 能找到函数。
    - `rg -n "where\\(\\{ leaseId: lease\\.id \\}\\)\\.remove" cloudfunctions/shared/repositories/bill-repository.ts` 没有结果。
    - 租约保存后已收账单、手工账单仍存在，旧未收系统账单被替换。
  </acceptance_criteria>
  <done>租约编辑后已收账单和手工账单保留，未收系统账单按新租约规则替换。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/cloud/dashboard-home.spec.ts tests/cloud/alerts-list.spec.ts --runInBand</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/cloud/dashboard-home.spec.ts tests/cloud/alerts-list.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- `syncBillsForLease` 不再按 `leaseId` 整批删除账单。
- `findById/updateRecord` 可在 `_id !== id` 的 mock 场景下通过测试。
- `alerts` 重建按当前房东隔离。
- 本计划不实现固定费用、水电、房东支出或安全删除 UI，只交付写入边界治理。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-费用与账单模型优化/05-01-SUMMARY.md`。
</output>
