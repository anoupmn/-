---
phase: "05-费用与账单模型优化"
plan: "05-02"
title: "固定费用、自定义费用与押金类一次性费用模型"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/cloud/bills-sync.spec.ts
  - tests/cloud/leases-save-billing.spec.ts
  - tests/domain/bill-status.spec.ts
  - tests/domain/rentable-unit-summary.spec.ts
  - cloudfunctions/shared/schemas/lease.ts
  - cloudfunctions/shared/schemas/bill.ts
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/shared/calculators/bill-status.ts
  - cloudfunctions/shared/calculators/rentable-unit.ts
  - miniprogram/pages/leases-form/index.ts
  - miniprogram/pages/leases-form/index.wxml
  - miniprogram/pages/leases-form/index.wxss
requirements: [FEE-01, FEE-02, FEE-03, FEE-04]
requirements_addressed: [FEE-01, FEE-02, FEE-03, FEE-04]
autonomous: true
must_haves:
  truths:
    - "租约固定费用包含租金、押金、管理费、消防押金、锁卡押金五项"
    - "消防押金和锁卡押金作为一次性押金类账单生成"
    - "押金类费用不进入逾期提醒，也不作为下一笔租金核心提示"
    - "自定义费用必须持久化费用性质"
  artifacts:
    - path: "cloudfunctions/shared/schemas/lease.ts"
      provides: "租约费用规则合同"
    - path: "cloudfunctions/shared/schemas/bill.ts"
      provides: "账单行分类合同"
    - path: "miniprogram/pages/leases-form/index.ts"
      provides: "固定费用和自定义费用性质录入"
  key_links:
    - from: "cloudfunctions/shared/schemas/lease.ts"
      to: "cloudfunctions/shared/repositories/bill-repository.ts"
      via: "getLeaseFeeRules 生成系统账单"
      pattern: "getLeaseFeeRules\\(lease\\)"
    - from: "cloudfunctions/shared/schemas/bill.ts"
      to: "cloudfunctions/shared/calculators/bill-status.ts"
      via: "feeNature/isDepositLike 决定逾期可追踪性"
      pattern: "feeNature|isDepositLike"
---

<objective>
把租约费用从旧的 `rent/deposit/property/misc/custom` 扩展为可解释的账单合同，固定内置五项和自定义费用性质都写入事实字段，供提醒、详情、后续导出和收据直接消费。

Purpose: 满足 D-11 到 D-17、D-36 到 D-38，并覆盖 `FEE-01`、`FEE-02`、`FEE-03`、`FEE-04`。
Output: 稳定的租约费用规则、账单行字段和租约表单录入能力。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DATA-COLLECTION-MATRIX.md
@.planning/phases/05-费用与账单模型优化/5-CONTEXT.md
@.planning/phases/05-费用与账单模型优化/05-RESEARCH.md
@.planning/phases/05-费用与账单模型优化/05-VALIDATION.md
@cloudfunctions/shared/schemas/lease.ts
@cloudfunctions/shared/schemas/bill.ts
@cloudfunctions/shared/repositories/bill-repository.ts
@miniprogram/pages/leases-form/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>05-02-01</id>
  <title>补齐固定费用与费用性质测试</title>
  <name>Task 1: 补齐固定费用与费用性质测试</name>
  <files>tests/cloud/bills-sync.spec.ts, tests/cloud/leases-save-billing.spec.ts, tests/domain/bill-status.spec.ts, tests/domain/rentable-unit-summary.spec.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/phases/05-费用与账单模型优化/05-VALIDATION.md
    - tests/cloud/bills-sync.spec.ts
    - tests/cloud/leases-save-billing.spec.ts
    - tests/domain/bill-status.spec.ts
    - tests/domain/rentable-unit-summary.spec.ts
  </read_first>
  <action>
    先写可 grep 的行为测试：
    - `tests/cloud/bills-sync.spec.ts` 新增 `generates fixed built-in fees with explicit fee nature`，断言系统账单包含 `rent`、`deposit`、`management`、`fire_deposit`、`lock_card_deposit`，并断言消防押金/锁卡押金为 `feeNature: 'deposit'`、`cadence: 'once'`、`isDepositLike: true`。
    - `tests/cloud/leases-save-billing.spec.ts` 新增 `allows management fee cadence to be one-time`，验证管理费默认 `cycle`，传入 `once` 后只生成起租日一笔。
    - `tests/domain/bill-status.spec.ts` 新增 `deposit-like bills do not become overdue`，不再只依赖 `type === 'deposit'`。
    - `tests/domain/rentable-unit-summary.spec.ts` 新增 `excludes deposit-like bills from next rent core prompt`，确保下一笔核心提示优先租金/周期性应收，不被消防押金或锁卡押金占用。
  </action>
  <acceptance_criteria>
    - `rg -n "generates fixed built-in fees with explicit fee nature" tests/cloud/bills-sync.spec.ts` 能找到测试。
    - `rg -n "allows management fee cadence to be one-time" tests/cloud/leases-save-billing.spec.ts` 能找到测试。
    - `rg -n "deposit-like bills do not become overdue" tests/domain/bill-status.spec.ts` 能找到测试。
    - `rg -n "excludes deposit-like bills from next rent core prompt" tests/domain/rentable-unit-summary.spec.ts` 能找到测试。
  </acceptance_criteria>
  <done>固定五项、管理费 cadence、自定义费用性质和押金类提醒排除测试均已写入。</done>
  <verify>
    <automated>rg -n "generates fixed built-in fees with explicit fee nature|allows management fee cadence to be one-time|deposit-like bills do not become overdue|excludes deposit-like bills from next rent core prompt" tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/domain/bill-status.spec.ts tests/domain/rentable-unit-summary.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>05-02-02</id>
  <title>扩展租约费用规则与账单行合同</title>
  <name>Task 2: 扩展租约费用规则与账单行合同</name>
  <files>cloudfunctions/shared/schemas/lease.ts, cloudfunctions/shared/schemas/bill.ts, cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/shared/calculators/bill-status.ts, cloudfunctions/shared/calculators/rentable-unit.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/schemas/lease.ts
    - cloudfunctions/shared/schemas/bill.ts
    - cloudfunctions/shared/repositories/bill-repository.ts
    - cloudfunctions/shared/calculators/bill-status.ts
    - cloudfunctions/shared/calculators/rentable-unit.ts
  </read_first>
  <action>
    实现后端合同，不靠 label 推断：
    - `billTypeSchema` 增加 `management`、`fire_deposit`、`lock_card_deposit`，保留 legacy `property` 读取兼容。
    - `billSchema` 增加 `feeNature: 'recurring' | 'one_time' | 'deposit'`、`responsibility: 'tenant'`、`cadence: 'cycle' | 'once'`、`isDepositLike`、`isOneTime`、`legacy`，并为旧账单提供 default/catch fallback。
    - `leaseFeeRulesSchema` 增加 `management`、`fireDeposit`、`lockCardDeposit`；`management` 默认 `{ amount: 0, cadence: 'cycle' }`，消防押金/锁卡押金默认 `{ amount: 0, cadence: 'once' }`。
    - `customFeeItemSchema` 增加必填 `feeNature`，取值为 `recurring`、`one_time`、`deposit`；新自定义费用不得缺省为猜测值，legacy 旧数据可在 normalizer 中 fallback 为 `recurring`。
    - `mapLeaseFeeItems` 生成账单时写入 `feeNature`、`cadence`、`isDepositLike`、`isOneTime`、`responsibility: 'tenant'`、`legacy`。
    - `isBillOverdueTrackable` 改为 `responsibility === 'tenant' && !isDepositLike && feeNature !== 'deposit'`。
    - `buildRentableUnitSummary` 的下一笔核心提示过滤押金类账单，避免消防押金/锁卡押金覆盖租金提示。
    - 不手工编辑生成的 `.js` 镜像；如项目构建流程会生成镜像，只通过既有构建/类型检查产出。
  </action>
  <acceptance_criteria>
    - `rg -n "fire_deposit|lock_card_deposit|management" cloudfunctions/shared/schemas/bill.ts cloudfunctions/shared/schemas/lease.ts` 能找到新增类型。
    - `rg -n "feeNature|responsibility|isDepositLike|isOneTime|legacy" cloudfunctions/shared/schemas/bill.ts` 能找到账单合同字段。
    - `rg -n "feeNature" cloudfunctions/shared/schemas/lease.ts cloudfunctions/shared/repositories/bill-repository.ts` 能找到自定义费用性质流转。
    - 押金类账单不会变成 `overdue`，也不会成为下一笔租金核心提示。
  </acceptance_criteria>
  <done>租约费用规则和账单行合同包含固定五项、费用性质、押金类和 legacy fallback 字段。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/domain/bill-status.spec.ts tests/domain/rentable-unit-summary.spec.ts --runInBand</automated>
  </verify>
</task>

<task type="auto">
  <id>05-02-03</id>
  <title>更新租约表单录入固定五项与自定义费用性质</title>
  <name>Task 3: 更新租约表单录入固定五项与自定义费用性质</name>
  <files>miniprogram/pages/leases-form/index.ts, miniprogram/pages/leases-form/index.wxml, miniprogram/pages/leases-form/index.wxss</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - miniprogram/pages/leases-form/index.ts
    - miniprogram/pages/leases-form/index.wxml
    - miniprogram/pages/leases-form/index.wxss
    - cloudfunctions/shared/schemas/lease.ts
  </read_first>
  <action>
    在现有朴素表单基础上补齐字段，不做高保真重设计：
    - 固定费用区展示并提交 `rent`、`deposit`、`management`、`fireDeposit`、`lockCardDeposit` 五项；消防押金和锁卡押金固定提交 `cadence: 'once'`、`feeNature: 'deposit'`。
    - 管理费默认 `cadence: 'cycle'`，提供可切换为 `once` 的控件，提交到 `feeRules.management.cadence`。
    - 自定义费用每一行必须选择 `feeNature`：`recurring` 文案“周期性费用”、`one_time` 文案“一次性费用”、`deposit` 文案“押金类费用”；未选择时阻止保存并提示“请选择自定义费用性质”。
    - 保存 payload 使用 `feeRules.customFeeItems[].feeNature`，不要靠 `label` 推断。
  </action>
  <acceptance_criteria>
    - `rg -n "fireDeposit|lockCardDeposit|management" miniprogram/pages/leases-form/index.ts miniprogram/pages/leases-form/index.wxml` 能找到固定五项字段。
    - `rg -n "recurring|one_time|deposit" miniprogram/pages/leases-form/index.ts miniprogram/pages/leases-form/index.wxml` 能找到自定义费用性质选项。
    - `rg -n "请选择自定义费用性质" miniprogram/pages/leases-form/index.ts` 能找到保存校验提示。
  </acceptance_criteria>
  <done>租约表单能提交固定五项和自定义费用性质，未选择性质时阻止保存。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/domain/bill-status.spec.ts tests/domain/rentable-unit-summary.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/bills-sync.spec.ts tests/cloud/leases-save-billing.spec.ts tests/domain/bill-status.spec.ts tests/domain/rentable-unit-summary.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- 固定内置费用五项生成正确账单。
- 自定义费用性质进入账单事实。
- 押金类账单进入账单事实，但不触发逾期或下一笔租金核心提示。
- 本计划不实现水电抄表、房东支出或安全删除。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-费用与账单模型优化/05-02-SUMMARY.md`。
</output>
