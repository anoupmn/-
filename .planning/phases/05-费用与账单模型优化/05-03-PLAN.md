---
phase: "05-费用与账单模型优化"
plan: "05-03"
title: "水电抄表计费、默认读数和月度账单展示"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/cloud/bills-save.spec.ts
  - tests/cloud/rentable-unit-detail-billing.spec.ts
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/shared/schemas/bill.ts
  - cloudfunctions/bills-save/index.ts
  - cloudfunctions/rentable-unit-detail/index.ts
  - miniprogram/services/bill.ts
  - miniprogram/pages/unit-detail/index.ts
  - miniprogram/pages/unit-detail/index.wxml
  - miniprogram/pages/unit-detail/index.wxss
requirements: [UTIL-01, UTIL-02]
requirements_addressed: [UTIL-01, UTIL-02]
autonomous: true
must_haves:
  truths:
    - "水电补录提交上期读数、本期读数和单价"
    - "水电金额由服务端计算并写入 bills"
    - "补录水电默认带出同房间同类型上一笔本期读数和单价"
    - "月度账单展示水电读数、用量、单价、金额和备注"
  artifacts:
    - path: "cloudfunctions/bills-save/index.ts"
      provides: "水电抄表写入口"
    - path: "cloudfunctions/shared/repositories/bill-repository.ts"
      provides: "服务端用量和金额计算"
    - path: "cloudfunctions/rentable-unit-detail/index.ts"
      provides: "默认读数和月度账单展示数据"
    - path: "miniprogram/pages/unit-detail/index.ts"
      provides: "水电抄表表单"
  key_links:
    - from: "miniprogram/services/bill.ts"
      to: "cloudfunctions/bills-save/index.ts"
      via: "saveBill payload"
      pattern: "previousReading|currentReading|unitPrice"
    - from: "cloudfunctions/rentable-unit-detail/index.ts"
      to: "miniprogram/pages/unit-detail/index.ts"
      via: "meterDefaults/monthlyBills"
      pattern: "meterDefaults|meterReading"
---

<objective>
把水费、电费从“手填金额”改为抄表计费：前端提交读数和单价，服务端计算用量和金额，详情页补录时带出上一笔读数和单价，并在月度账单中展示计算结果。

Purpose: 满足 D-18、D-19、D-20、D-21，并覆盖 `UTIL-01`、`UTIL-02`。
Output: 服务端金额真相、默认读数查询和可用的详情页水电补录体验。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DATA-COLLECTION-MATRIX.md
@.planning/phases/05-费用与账单模型优化/5-CONTEXT.md
@.planning/phases/05-费用与账单模型优化/05-RESEARCH.md
@.planning/phases/05-费用与账单模型优化/05-VALIDATION.md
@cloudfunctions/bills-save/index.ts
@cloudfunctions/rentable-unit-detail/index.ts
@miniprogram/pages/unit-detail/index.ts
@miniprogram/services/bill.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>05-03-01</id>
  <title>补齐水电抄表 Wave 0 测试</title>
  <name>Task 1: 补齐水电抄表 Wave 0 测试</name>
  <files>tests/cloud/bills-save.spec.ts, tests/cloud/rentable-unit-detail-billing.spec.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/phases/05-费用与账单模型优化/05-VALIDATION.md
    - tests/cloud/bills-save.spec.ts
    - tests/cloud/rentable-unit-detail-billing.spec.ts
  </read_first>
  <action>
    写明水电行为测试：
    - `tests/cloud/bills-save.spec.ts` 新增 `calculates utility amount from meter readings on server`，调用 `bills-save` 创建 `type: 'water'`，传 `previousReading: 100`、`currentReading: 135.5`、`unitPrice: 3.2`，断言 `meterReading.usage === 35.5`、`amount === 113.6`。
    - 同文件新增 `rejects utility bill when current reading is lower than previous reading`，断言服务端报错，不写入账单。
    - 同文件新增 `keeps utility note and does not trust client amount`，传入伪造 `amount: 1` 时最终金额仍按读数计算，`note` 原样保存。
    - `tests/cloud/rentable-unit-detail-billing.spec.ts` 新增 `returns previous meter reading defaults for water and electricity`，断言同房间同类型上一笔水电账单的 `currentReading` 和 `unitPrice` 作为默认值返回。
  </action>
  <acceptance_criteria>
    - `rg -n "calculates utility amount from meter readings on server" tests/cloud/bills-save.spec.ts` 能找到测试。
    - `rg -n "rejects utility bill when current reading is lower than previous reading" tests/cloud/bills-save.spec.ts` 能找到测试。
    - `rg -n "keeps utility note and does not trust client amount" tests/cloud/bills-save.spec.ts` 能找到测试。
    - `rg -n "returns previous meter reading defaults for water and electricity" tests/cloud/rentable-unit-detail-billing.spec.ts` 能找到测试。
  </acceptance_criteria>
  <done>水电服务端计算、读数校验、备注保留和默认读数测试均已写入。</done>
  <verify>
    <automated>rg -n "calculates utility amount from meter readings on server|rejects utility bill when current reading is lower than previous reading|keeps utility note and does not trust client amount|returns previous meter reading defaults for water and electricity" tests/cloud/bills-save.spec.ts tests/cloud/rentable-unit-detail-billing.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>05-03-02</id>
  <title>实现服务端水电抄表账单与默认读数</title>
  <name>Task 2: 实现服务端水电抄表账单与默认读数</name>
  <files>cloudfunctions/shared/schemas/bill.ts, cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/bills-save/index.ts, cloudfunctions/rentable-unit-detail/index.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/schemas/bill.ts
    - cloudfunctions/shared/repositories/bill-repository.ts
    - cloudfunctions/bills-save/index.ts
    - cloudfunctions/rentable-unit-detail/index.ts
  </read_first>
  <action>
    在后端完成真相计算：
    - `billSchema` 的 `meterReading` 字段包含 `previousReading`、`currentReading`、`usage`、`unitPrice`，仅水费/电费账单要求存在。
    - 在 `bill-repository.ts` 新增 `calculateMeterBill({ previousReading, currentReading, unitPrice })`，校验三者为非负数，`currentReading >= previousReading`，金额使用 `Math.round(usage * unitPrice * 100) / 100`。
    - 新增 `createMeterBill` 或扩展 `createManualBill`，水电类型必须走读数计算；`bills-save` 对 `type: 'water' | 'electricity'` 忽略客户端 `amount`，只接收读数、单价和 `note`。
    - `bills-save` 对非水电租客应收仍允许 `amount`，但不得接受维修、保洁、打理、请人管理等房东支出标签写入 `bills`。
    - 在 `rentable-unit-detail` 中返回 `meterDefaults.water`、`meterDefaults.electricity`，来源为同房间同类型最近一笔含 `meterReading` 的账单；默认值包含 `previousReading` 和 `unitPrice`。
    - 不手工编辑生成的 `.js` 镜像；如项目构建流程会生成镜像，只通过既有构建/类型检查产出。
  </action>
  <acceptance_criteria>
    - `rg -n "meterReading|previousReading|currentReading|unitPrice|usage" cloudfunctions/shared/schemas/bill.ts cloudfunctions/shared/repositories/bill-repository.ts cloudfunctions/bills-save/index.ts` 能找到字段和计算。
    - `rg -n "Math\\.round\\(.*unitPrice.*100\\).*100" cloudfunctions/shared/repositories/bill-repository.ts` 能找到金额取两位逻辑。
    - `rg -n "meterDefaults" cloudfunctions/rentable-unit-detail/index.ts` 能找到默认读数输出。
    - 水电测试全部通过。
  </acceptance_criteria>
  <done>水电账单金额由服务端读数计算，详情接口返回上一笔读数和单价默认值。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/bills-save.spec.ts tests/cloud/rentable-unit-detail-billing.spec.ts --runInBand</automated>
  </verify>
</task>

<task type="auto">
  <id>05-03-03</id>
  <title>更新单元详情水电补录与月度账单展示</title>
  <name>Task 3: 更新单元详情水电补录与月度账单展示</name>
  <files>miniprogram/services/bill.ts, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/pages/unit-detail/index.wxss</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - miniprogram/services/bill.ts
    - miniprogram/pages/unit-detail/index.ts
    - miniprogram/pages/unit-detail/index.wxml
    - miniprogram/pages/unit-detail/index.wxss
    - cloudfunctions/rentable-unit-detail/index.ts
  </read_first>
  <action>
    在现有 `unit-detail` 月度账单和补录入口上改造：
    - `saveBill` 类型支持水电 payload：`previousReading`、`currentReading`、`unitPrice`、`note`；前端不提交水电最终 `amount` 作为真相。
    - 点击补录水费/电费时，从详情数据 `meterDefaults[type]` 填充上期读数和单价，本期读数为空等待输入。
    - 表单校验：水电必须填上期读数、本期读数、单价；本期读数小于上期读数时前端提示，但最终仍以服务端校验为准。
    - 月度账单 item 若有 `meterReading`，展示 `上期读数`、`本期读数`、`用量`、`单价`、`金额`、`备注`；普通账单继续展示金额和备注。
  </action>
  <acceptance_criteria>
    - `rg -n "previousReading|currentReading|unitPrice" miniprogram/services/bill.ts miniprogram/pages/unit-detail/index.ts miniprogram/pages/unit-detail/index.wxml` 能找到水电字段。
    - `rg -n "meterDefaults" miniprogram/pages/unit-detail/index.ts` 能找到默认值消费。
    - `rg -n "上期读数|本期读数|用量|单价" miniprogram/pages/unit-detail/index.wxml` 能找到展示文案。
    - `rg -n "amount" miniprogram/services/bill.ts` 不应把水电最终金额列为必传字段。
  </acceptance_criteria>
  <done>单元详情页水电补录不提交最终金额为真相，并展示读数、用量、单价、金额和备注。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/bills-save.spec.ts tests/cloud/rentable-unit-detail-billing.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/bills-save.spec.ts tests/cloud/rentable-unit-detail-billing.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- 水费、电费补录由服务端计算金额。
- 详情页默认带出上一笔同类型水电读数和单价。
- 月度账单能看见水电读数、用量、单价、金额和备注。
- 本计划不把维修、保洁或打理写入 `bills`。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-费用与账单模型优化/05-03-SUMMARY.md`。
</output>
