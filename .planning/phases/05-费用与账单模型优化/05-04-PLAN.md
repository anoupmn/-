---
phase: "05-费用与账单模型优化"
plan: "05-04"
title: "维修/打理支出独立建模与房间问题分析口径"
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - tests/helpers/mock-cloud.ts
  - tests/cloud/owner-expense-save.spec.ts
  - tests/cloud/repair-record-save.spec.ts
  - tests/cloud/alerts-list.spec.ts
  - cloudfunctions/shared/constants/collections.ts
  - cloudfunctions/shared/schemas/owner-expense.ts
  - cloudfunctions/shared/repositories/owner-expense-repository.ts
  - cloudfunctions/shared/repositories/repair-record-repository.ts
  - cloudfunctions/owner-expense-save/index.ts
  - cloudfunctions/owner-expense-save/package.json
  - cloudfunctions/rentable-unit-detail/index.ts
  - miniprogram/services/owner-expense.ts
  - miniprogram/pages/unit-detail/index.ts
  - miniprogram/pages/unit-detail/index.wxml
  - miniprogram/pages/unit-detail/index.wxss
requirements: [OPEX-01, OPEX-02]
requirements_addressed: [OPEX-01, OPEX-02]
autonomous: true
must_haves:
  truths:
    - "维修、保洁、打理、请人管理等房东支出不写入 bills"
    - "房东支出写入 owner_expenses，金额可以为空"
    - "维修类支出可以关联维修事实并参与房间问题分析"
    - "保洁、打理、请人管理和其他支出不计入维修高频异常"
  artifacts:
    - path: "cloudfunctions/shared/schemas/owner-expense.ts"
      provides: "房东支出事实合同"
    - path: "cloudfunctions/shared/repositories/owner-expense-repository.ts"
      provides: "房东支出保存和查询"
    - path: "cloudfunctions/owner-expense-save/index.ts"
      provides: "记录维修/支出云函数"
    - path: "miniprogram/pages/unit-detail/index.ts"
      provides: "记录维修/支出入口"
  key_links:
    - from: "cloudfunctions/owner-expense-save/index.ts"
      to: "owner_expenses"
      via: "createOwnerExpense"
      pattern: "createOwnerExpense"
    - from: "cloudfunctions/shared/repositories/owner-expense-repository.ts"
      to: "cloudfunctions/shared/repositories/repair-record-repository.ts"
      via: "expenseType === 'repair' 时创建/关联 repair_records"
      pattern: "expenseType.*repair"
---

<objective>
新增房东经营支出事实口径，让维修、保洁、打理、请人管理等支出从租客账单中剥离；只有维修类事实继续参与问题房间和高频维修异常分析。

Purpose: 满足 D-02、D-03、D-22 到 D-27，并覆盖 `OPEX-01`、`OPEX-02`。
Output: `owner_expenses` 集合合同、保存云函数、详情页入口和问题分析过滤口径。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DATA-COLLECTION-MATRIX.md
@.planning/phases/05-费用与账单模型优化/5-CONTEXT.md
@.planning/phases/05-费用与账单模型优化/05-RESEARCH.md
@.planning/phases/05-费用与账单模型优化/05-VALIDATION.md
@cloudfunctions/shared/repositories/repair-record-repository.ts
@cloudfunctions/repair-record-save/index.ts
@miniprogram/services/repair.ts
@miniprogram/pages/unit-detail/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>05-04-01</id>
  <title>补齐房东支出 Wave 0 测试</title>
  <name>Task 1: 补齐房东支出 Wave 0 测试</name>
  <files>tests/helpers/mock-cloud.ts, tests/cloud/owner-expense-save.spec.ts, tests/cloud/repair-record-save.spec.ts, tests/cloud/alerts-list.spec.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/phases/05-费用与账单模型优化/05-VALIDATION.md
    - tests/helpers/mock-cloud.ts
    - tests/cloud/repair-record-save.spec.ts
    - tests/cloud/alerts-list.spec.ts
  </read_first>
  <action>
    新增/扩展测试：
    - `tests/helpers/mock-cloud.ts` 增加 `ownerExpenses` store 映射到集合名 `owner_expenses`。
    - 新建 `tests/cloud/owner-expense-save.spec.ts`，包含测试名 `creates owner expense with optional amount`、`links repair expense to repair record`、`does not create repair record for cleaning caretaking labor or other`、`groups expenses by occurredAt month for export contract`。
    - `tests/cloud/repair-record-save.spec.ts` 或 `tests/cloud/alerts-list.spec.ts` 增加 `non-repair owner expenses do not affect repair anomaly stats`，断言保洁/打理/请人管理不会增加维修高频异常。
  </action>
  <acceptance_criteria>
    - `rg -n "ownerExpenses|owner_expenses" tests/helpers/mock-cloud.ts` 能找到集合映射。
    - `test -f tests/cloud/owner-expense-save.spec.ts` 成功。
    - `rg -n "creates owner expense with optional amount|links repair expense to repair record|does not create repair record for cleaning caretaking labor or other|groups expenses by occurredAt month for export contract" tests/cloud/owner-expense-save.spec.ts` 能找到测试名。
    - `rg -n "non-repair owner expenses do not affect repair anomaly stats" tests/cloud/repair-record-save.spec.ts tests/cloud/alerts-list.spec.ts` 能找到测试。
  </acceptance_criteria>
  <done>房东支出集合 mock、保存测试、维修关联和非维修排除异常测试均已写入。</done>
  <verify>
    <automated>rg -n "ownerExpenses|owner_expenses|creates owner expense with optional amount|links repair expense to repair record|does not create repair record for cleaning caretaking labor or other|groups expenses by occurredAt month for export contract|non-repair owner expenses do not affect repair anomaly stats" tests/helpers/mock-cloud.ts tests/cloud/owner-expense-save.spec.ts tests/cloud/repair-record-save.spec.ts tests/cloud/alerts-list.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>05-04-02</id>
  <title>新增 owner_expenses 合同、repository 与云函数</title>
  <name>Task 2: 新增 owner_expenses 合同、repository 与云函数</name>
  <files>cloudfunctions/shared/constants/collections.ts, cloudfunctions/shared/schemas/owner-expense.ts, cloudfunctions/shared/repositories/owner-expense-repository.ts, cloudfunctions/shared/repositories/repair-record-repository.ts, cloudfunctions/owner-expense-save/index.ts, cloudfunctions/owner-expense-save/package.json, cloudfunctions/rentable-unit-detail/index.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/constants/collections.ts
    - cloudfunctions/shared/repositories/repair-record-repository.ts
    - cloudfunctions/repair-record-save/index.ts
    - cloudfunctions/rentable-unit-detail/index.ts
  </read_first>
  <action>
    实现独立支出事实：
    - `COLLECTIONS` 增加 `ownerExpenses: 'owner_expenses'`。
    - 新增 `ownerExpenseSchema`，字段为 `id`、`landlordOpenId`、`assetId`、`roomId`、`leaseId`、`tenantId`、`repairRecordId`、`expenseType`、`amount`、`note`、`occurredAt`、`monthKey`、`createdAt`、`updatedAt`；`expenseType` 取值 `repair | cleaning | caretaking | labor | other`，`amount` 为 `number | null`。
    - 新增 `owner-expense-repository.ts`，导出 `createOwnerExpense`、`listOwnerExpenses`、`listOwnerExpensesByRoom`；`monthKey` 从 `occurredAt.slice(0, 7)` 写入，为 Phase 06 导出准备。
    - `owner-expense-save` 云函数校验房间归属当前 `landlordOpenId`；`expenseType === 'repair'` 时创建或关联一条 `repair_records`，非维修类型只写 `owner_expenses`。
    - `amount` 为空时仍保存事实；金额统计时只汇总 `amount !== null`。
    - `rentable-unit-detail` 返回当前房间支出摘要和最近支出列表，但不得在查询时写任何真相集合。
    - 不手工编辑生成的 `.js` 镜像；如项目构建流程会生成镜像，只通过既有构建/类型检查产出。
  </action>
  <acceptance_criteria>
    - `rg -n "ownerExpenses: 'owner_expenses'" cloudfunctions/shared/constants/collections.ts` 能找到集合常量。
    - `test -f cloudfunctions/shared/schemas/owner-expense.ts` 和 `test -f cloudfunctions/shared/repositories/owner-expense-repository.ts` 成功。
    - `rg -n "repair|cleaning|caretaking|labor|other" cloudfunctions/shared/schemas/owner-expense.ts` 能找到支出类型枚举。
    - `rg -n "monthKey|occurredAt" cloudfunctions/shared/repositories/owner-expense-repository.ts` 能找到按发生日期归月字段。
    - 非维修类支出不会创建 `repair_records`。
  </acceptance_criteria>
  <done>`owner_expenses` 合同、repository 和保存云函数可创建金额可空的房东支出，且非维修不写维修事实。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/owner-expense-save.spec.ts tests/cloud/repair-record-save.spec.ts tests/cloud/alerts-list.spec.ts --runInBand</automated>
  </verify>
</task>

<task type="auto">
  <id>05-04-03</id>
  <title>接入详情页记录维修/支出入口</title>
  <name>Task 3: 接入详情页记录维修/支出入口</name>
  <files>miniprogram/services/owner-expense.ts, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/pages/unit-detail/index.wxss</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - miniprogram/pages/unit-detail/index.ts
    - miniprogram/pages/unit-detail/index.wxml
    - miniprogram/pages/unit-detail/index.wxss
    - miniprogram/services/repair.ts
    - cloudfunctions/shared/schemas/owner-expense.ts
  </read_first>
  <action>
    在详情页提供合并入口但保持后端事实拆分：
    - 新增 `miniprogram/services/owner-expense.ts`，导出 `saveOwnerExpense(payload)`，调用云函数名 `owner-expense-save`。
    - 将详情页原“记录维修”入口调整为“记录维修/支出”；分类包括 `repair` 文案“维修”、`cleaning` 文案“保洁”、`caretaking` 文案“打理”、`labor` 文案“请人管理”、`other` 文案“其他支出”。
    - 表单字段包括发生日期 `occurredAt`、分类 `expenseType`、金额 `amount`（可空）、备注 `note`；维修类保留维修分类或备注要求，非维修类不要求维修分类。
    - 提交后刷新 `rentable-unit-detail`，并在详情页展示房东支出列表；不要调用 `saveBill` 写 `bills`。
  </action>
  <acceptance_criteria>
    - `test -f miniprogram/services/owner-expense.ts` 成功。
    - `rg -n "owner-expense-save|saveOwnerExpense" miniprogram/services/owner-expense.ts miniprogram/pages/unit-detail/index.ts` 能找到服务调用。
    - `rg -n "维修/支出|保洁|打理|请人管理|其他支出" miniprogram/pages/unit-detail/index.wxml miniprogram/pages/unit-detail/index.ts` 能找到入口文案和分类。
    - `rg -n "saveBill\\(" miniprogram/pages/unit-detail/index.ts` 不应出现在维修/保洁/打理/请人管理提交路径中。
  </acceptance_criteria>
  <done>详情页通过 `owner-expense-save` 记录维修/支出，不再把房东支出写入 `bills`。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/owner-expense-save.spec.ts tests/cloud/repair-record-save.spec.ts tests/cloud/alerts-list.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/owner-expense-save.spec.ts tests/cloud/repair-record-save.spec.ts tests/cloud/alerts-list.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- 房东支出进入 `owner_expenses`，不进入 `bills`。
- 金额为空仍可留痕，金额不为空可供统计和后续导出。
- 只有维修类支出参与维修问题分析。
- 详情页可记录维修/支出并展示结果。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-费用与账单模型优化/05-04-SUMMARY.md`。
</output>
