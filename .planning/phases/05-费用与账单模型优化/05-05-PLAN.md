---
phase: "05-费用与账单模型优化"
plan: "05-05"
title: "退租、录错、删除/修改补救策略与全链路验证"
type: execute
wave: 5
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - tests/cloud/leases-delete.spec.ts
  - tests/cloud/unit-detail-flow.spec.ts
  - tests/cloud/bills-sync.spec.ts
  - tests/cloud/bills-save.spec.ts
  - tests/cloud/owner-expense-save.spec.ts
  - cloudfunctions/shared/constants/collections.ts
  - cloudfunctions/shared/repositories/lease-repository.ts
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/leases-delete/index.ts
  - cloudfunctions/leases-delete/package.json
  - cloudfunctions/leases-end/index.ts
  - miniprogram/services/lease.ts
  - miniprogram/pages/unit-detail/index.ts
  - miniprogram/pages/unit-detail/index.wxml
  - miniprogram/pages/unit-detail/index.wxss
requirements: [CORR-02]
requirements_addressed: [CORR-02]
autonomous: true
must_haves:
  truths:
    - "无已收账单、无收据、无维修/支出关联的租约可以安全删除"
    - "安全删除租约会级联删除该租约下未收款且无收据引用的账单"
    - "已有已收账单、收据、维修记录或房东支出关联的租约禁止硬删除"
    - "退租继续走结束租约，并提示未收账单处理选择"
    - "Phase 05 全链路测试和 typecheck 通过"
  artifacts:
    - path: "cloudfunctions/leases-delete/index.ts"
      provides: "租约安全删除云函数"
    - path: "cloudfunctions/shared/repositories/lease-repository.ts"
      provides: "删除 blocker 和级联删除规则"
    - path: "miniprogram/pages/unit-detail/index.ts"
      provides: "安全删除和结束租约补救入口"
    - path: "tests/cloud/leases-delete.spec.ts"
      provides: "租约删除保护测试"
  key_links:
    - from: "miniprogram/services/lease.ts"
      to: "cloudfunctions/leases-delete/index.ts"
      via: "deleteLease"
      pattern: "leases-delete"
    - from: "cloudfunctions/shared/repositories/lease-repository.ts"
      to: "bills/repair_records/owner_expenses/receipts"
      via: "delete blocker 检查"
      pattern: "paid_bill|receipt|repair_record|owner_expense"
---

<objective>
完成租约录错、退租和删除补救策略：允许无历史关联租约安全删除，保护已有收款/收据/维修/支出关联的租约，并做 Phase 05 全链路验证。

Purpose: 满足 D-28 到 D-35，并覆盖 `CORR-02`。
Output: `leases-delete` 云函数、详情页补救入口、删除 blocker 测试和阶段级回归验证。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DATA-COLLECTION-MATRIX.md
@.planning/phases/05-费用与账单模型优化/5-CONTEXT.md
@.planning/phases/05-费用与账单模型优化/05-RESEARCH.md
@.planning/phases/05-费用与账单模型优化/05-VALIDATION.md
@cloudfunctions/shared/repositories/lease-repository.ts
@cloudfunctions/shared/repositories/bill-repository.ts
@cloudfunctions/leases-end/index.ts
@miniprogram/services/lease.ts
@miniprogram/pages/unit-detail/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>05-05-01</id>
  <title>补齐安全删除和退租补救测试</title>
  <name>Task 1: 补齐安全删除和退租补救测试</name>
  <files>tests/cloud/leases-delete.spec.ts, tests/cloud/unit-detail-flow.spec.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/phases/05-费用与账单模型优化/05-VALIDATION.md
    - cloudfunctions/shared/repositories/lease-repository.ts
    - cloudfunctions/shared/repositories/bill-repository.ts
    - tests/cloud/leases-end.spec.ts
  </read_first>
  <action>
    新增安全删除测试：
    - 新建 `tests/cloud/leases-delete.spec.ts`，包含 `deletes lease and unpaid bills when there are no blockers`，断言租约删除且该租约未收账单被删除。
    - 同文件新增 `blocks hard delete when lease has paid bill`、`blocks hard delete when lease has receipt reference`、`blocks hard delete when lease has repair record`、`blocks hard delete when lease has owner expense`，断言返回 blocker code 分别为 `paid_bill`、`receipt`、`repair_record`、`owner_expense`。
    - 同文件新增 `requires explicit confirm before destructive delete`，先 `mode: 'check'` 或缺少 `confirm: true` 时只返回可删除摘要，不实际删除。
    - 新建或扩展 `tests/cloud/unit-detail-flow.spec.ts`，使用静态或服务层测试断言详情页存在 `deleteLease` 服务调用和未收账单处理提示文案。
  </action>
  <acceptance_criteria>
    - `test -f tests/cloud/leases-delete.spec.ts` 成功。
    - `rg -n "deletes lease and unpaid bills when there are no blockers|blocks hard delete when lease has paid bill|blocks hard delete when lease has receipt reference|blocks hard delete when lease has repair record|blocks hard delete when lease has owner expense|requires explicit confirm before destructive delete" tests/cloud/leases-delete.spec.ts` 能找到测试名。
    - `test -f tests/cloud/unit-detail-flow.spec.ts` 成功。
    - `rg -n "deleteLease|保留欠款|作废未收系统账单|修改截止日期后重算" tests/cloud/unit-detail-flow.spec.ts` 能找到详情页补救断言。
  </acceptance_criteria>
  <done>租约安全删除 blocker、确认删除和详情页补救提示测试均已写入。</done>
  <verify>
    <automated>rg -n "deletes lease and unpaid bills when there are no blockers|blocks hard delete when lease has paid bill|blocks hard delete when lease has receipt reference|blocks hard delete when lease has repair record|blocks hard delete when lease has owner expense|requires explicit confirm before destructive delete|deleteLease|保留欠款|作废未收系统账单|修改截止日期后重算" tests/cloud/leases-delete.spec.ts tests/cloud/unit-detail-flow.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>05-05-02</id>
  <title>实现 leases-delete 与结束租约补救语义</title>
  <name>Task 2: 实现 leases-delete 与结束租约补救语义</name>
  <files>cloudfunctions/shared/constants/collections.ts, cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/leases-delete/index.ts, cloudfunctions/leases-delete/package.json, cloudfunctions/leases-end/index.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/constants/collections.ts
    - cloudfunctions/shared/repositories/lease-repository.ts
    - cloudfunctions/shared/repositories/bill-repository.ts
    - cloudfunctions/leases-end/index.ts
  </read_first>
  <action>
    实现后端删除和退租保护：
    - `COLLECTIONS` 增加 `receipts: 'receipts'`，用于检查 Phase 06 收据引用；集合不存在时按空集合处理。
    - 在 `lease-repository.ts` 新增 `getLeaseDeleteBlockers(db, leaseId, landlordOpenId)`，检查该租约下是否存在：已收账单（`receivedAt` 且 `receivedAmount !== null`）、任意 `receiptId` 或 `receipts` 集合引用、`repair_records.leaseId`、`owner_expenses.leaseId`。
    - 新增 `deleteLeaseSafely(db, leaseId, landlordOpenId, { confirm })`：无 blocker 且 `confirm === true` 时删除租约，并删除该租约下未收款且无收据引用的账单；否则返回 `{ canDelete, blockers, unpaidBillCount }`。
    - 新增云函数 `cloudfunctions/leases-delete/index.ts`，校验当前房东拥有该租约，支持 `mode: 'check' | 'delete'` 和 `confirm: boolean`。
    - `leases-end` 保持“结束租约”语义，不硬删历史；如存在未收系统账单，返回 `unpaidBillSummary` 和建议选项 `keep_debt`、`void_unpaid_system_bills`、`adjust_end_date_and_resync`，不要自动替用户删除。
    - 不手工编辑生成的 `.js` 镜像；如项目构建流程会生成镜像，只通过既有构建/类型检查产出。
  </action>
  <acceptance_criteria>
    - `test -f cloudfunctions/leases-delete/index.ts` 成功。
    - `rg -n "getLeaseDeleteBlockers|deleteLeaseSafely|paid_bill|receipt|repair_record|owner_expense" cloudfunctions/shared/repositories/lease-repository.ts` 能找到 blocker 逻辑。
    - `rg -n "receipts: 'receipts'" cloudfunctions/shared/constants/collections.ts` 能找到集合常量。
    - `rg -n "keep_debt|void_unpaid_system_bills|adjust_end_date_and_resync" cloudfunctions/leases-end/index.ts cloudfunctions/shared/repositories/lease-repository.ts` 能找到退租未收账单处理选项。
  </acceptance_criteria>
  <done>`leases-delete` 能区分可删除租约和 blocker 租约，结束租约返回未收账单处理选项。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/leases-delete.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand</automated>
  </verify>
</task>

<task type="auto">
  <id>05-05-03</id>
  <title>接入详情页补救入口并做 Phase 05 全链路回归</title>
  <name>Task 3: 接入详情页补救入口并做 Phase 05 全链路回归</name>
  <files>miniprogram/services/lease.ts, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/pages/unit-detail/index.wxss, tests/cloud/bills-sync.spec.ts, tests/cloud/bills-save.spec.ts, tests/cloud/owner-expense-save.spec.ts</files>
  <read_first>
    - .planning/phases/05-费用与账单模型优化/5-CONTEXT.md
    - .planning/phases/05-费用与账单模型优化/05-VALIDATION.md
    - miniprogram/services/lease.ts
    - miniprogram/pages/unit-detail/index.ts
    - miniprogram/pages/unit-detail/index.wxml
    - tests/cloud/bills-sync.spec.ts
    - tests/cloud/bills-save.spec.ts
    - tests/cloud/owner-expense-save.spec.ts
  </read_first>
  <action>
    完成前端补救入口和阶段回归：
    - `miniprogram/services/lease.ts` 新增 `deleteLease(payload)`，调用云函数 `leases-delete`。
    - 详情页租约操作区新增“安全删除租约”入口：先调用 `mode: 'check'`，无 blocker 时弹二次确认并用 `confirm: true` 删除；有 blocker 时展示禁止硬删除原因，并提示“编辑、更正、结束或作废”。
    - 结束租约流程展示未收账单处理提示，文案固定包含“保留欠款”、“作废未收系统账单”、“修改截止日期后重算”，第一版只提示，不实现复杂迁移。
    - 做 Phase 05 回归：账单同步、固定费用、水电抄表、房东支出和安全删除测试全部通过；不要实现 Phase 06 导出或收据生成 UI。
  </action>
  <acceptance_criteria>
    - `rg -n "leases-delete|deleteLease" miniprogram/services/lease.ts miniprogram/pages/unit-detail/index.ts` 能找到前端调用。
    - `rg -n "安全删除租约|编辑、更正、结束或作废|保留欠款|作废未收系统账单|修改截止日期后重算" miniprogram/pages/unit-detail/index.ts miniprogram/pages/unit-detail/index.wxml` 能找到补救文案。
    - `npm test -- --runInBand` 通过。
    - `npm run typecheck` 通过。
  </acceptance_criteria>
  <done>详情页接入安全删除和退租提示，Phase 05 全量测试与 typecheck 通过。</done>
  <verify>
    <automated>npm test -- --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/leases-delete.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand`
- `npm test -- --runInBand && npm run typecheck`
</verification>

<success_criteria>
- 无 blocker 租约可安全删除，未收且无收据引用账单被级联删除。
- 有已收账单、收据、维修记录或房东支出关联时禁止硬删除。
- 退租走结束租约，不丢历史。
- Phase 05 关键测试和 typecheck 全部通过。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-费用与账单模型优化/05-05-SUMMARY.md`。
</output>
