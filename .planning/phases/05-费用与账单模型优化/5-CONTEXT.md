# 第 5 阶段：费用与账单模型优化 - 上下文

**收集时间：** 2026-04-27  
**状态：** 可进入规划

<domain>
## 阶段边界

本阶段交付“费用与账单模型优化”，也就是在既有房源、房间、租约、账单、维修记录和首页提醒能力之上，稳定固定费用、水电抄表、房东维修/打理支出、租约录错补救和集合写入边界。

本阶段不做月度导出和收据 UI/文件生成，这些属于第 6 阶段。本阶段也不做 Excel 批量导入和微信官方提醒消息实际发送闭环；这两项已从当前路线删除。

本阶段的核心目标是让后续导出和收据有稳定数据基础：租客应收归租客应收，房东支出归房东支出，已收账单和历史凭证不能被编辑租约时静默删除。

</domain>

<decisions>
## 实现决策

### 数据集合与写入边界
- **D-01：** 第 5 阶段必须先审计数据集合职责和各种操作对应的数据修改，发现读写冲突要优先修正。
- **D-02：** `bills` 只承载租客应收/实收账单；维修、保洁、打理、请人管理等房东支出不得写入 `bills`。
- **D-03：** 房东支出进入独立事实口径，推荐新增 `owner_expenses` 集合。
- **D-04：** `alerts` 是派生集合，可以重建，但必须按当前房东范围清理和写入，不能为一个用户打开首页清空全局提醒。
- **D-05：** `rentable-unit-detail` 和 `rentable-units-list` 等展示型读侧函数不得补写或重建账单。
- **D-06：** 业务读写优先通过业务 `id`，不得假设业务 `id` 与数据库 `_id` 一致；Phase 05 需要审计并修正 `findById/updateRecord` 相关风险。

### 账单同步安全
- **D-07：** 编辑租约时不得按 `leaseId` 整批删除所有账单。
- **D-08：** `syncBillsForLease` 只能替换未收款的系统生成账单。
- **D-09：** 已收账单永远保留；手工补录账单永远保留，除非走明确删除/作废流程。
- **D-10：** 后续导出和收据依赖账单历史稳定，因此禁止“保存租约后悄悄抹掉旧账单”的行为。

### 固定费用
- **D-11：** 租约固定内置费用为：租金、押金、管理费、消防押金、锁卡押金。
- **D-12：** 消防押金和锁卡押金是一次性押金类应收，进入账单、收据和导出。
- **D-13：** 消防押金和锁卡押金不参与逾期提醒，也不作为“下一笔租金”核心提示。
- **D-14：** 管理费默认是周期性费用，但允许在租约里改为一次性费用。

### 自定义费用
- **D-15：** 自定义费用必须选择性质：周期性费用、一次性费用或押金类费用。
- **D-16：** 自定义费用性质需要进入账单事实，后续详情、收款、导出和收据不能靠 label 猜测。
- **D-17：** 押金类自定义费用进入账单/收据/导出，但不进入逾期提醒或下一笔租金核心提示。

### 水电抄表
- **D-18：** 水电费从“手填金额”改为“上期抄数、本期抄数、单价”，由服务端计算用量和金额。
- **D-19：** 水电补录时默认带出同房间同类型上一笔账单的本期读数和单价，降低重复录入。
- **D-20：** 水电账单保留备注，便于记录特殊情况。
- **D-21：** 前端不得自行计算最终账单金额作为真相；服务端计算结果写入 `bills`。

### 维修与房东支出
- **D-22：** 维修、灯具、墙面、厕所堵塞、保洁、打理、请人管理等费用均是房东经营支出，不进入租客应收账单。
- **D-23：** 维修/支出入口可以合并为“记录维修/支出”，内部用分类区分维修类、保洁/打理类和其他支出。
- **D-24：** 金额不必填；不填金额也可以留维修事实，填了金额才进入支出统计和月度导出。
- **D-25：** 只有真正维修类记录参与“问题房间/高频维修异常”分析。
- **D-26：** 保洁、打理、请人管理等支出不计入维修高频异常。
- **D-27：** 房东支出后续需要支持月度导出，按发生日期归月。

### 退租与录错补救
- **D-28：** 房源、房间、租客基础信息录错允许直接编辑。
- **D-29：** 没有已收账单、没有收据、没有维修/支出关联的租约允许安全删除。
- **D-30：** 安全删除租约时级联删除该租约下未收款账单。
- **D-31：** 一旦租约产生已收账单、收据或维修/支出关联，则禁止硬删除，只能编辑、更正、结束或作废。
- **D-32：** 租期或金额录错且未收款时，优先允许直接编辑并重算未收款系统账单。
- **D-33：** 房间或租客选错且未收款时，第一版允许删除重建，不强行做复杂迁移。
- **D-34：** 退租走“结束租约”，保留历史租约、历史账单、历史维修/支出。
- **D-35：** 退租时如存在未收账单，需要提示处理：保留欠款、作废未收系统账单、或修改截止日期后重算。

### 与 Phase 06 的数据合同
- **D-36：** 第 5 阶段要产出后续导出和收据可直接消费的账单行合同。
- **D-37：** 账单行合同至少需要表达费用类型、费用性质、责任方、是否押金类、是否一次性、是否系统生成、是否历史/legacy、水电读数和收款状态。
- **D-38：** 月度导出和收据不得重新推导账单分类，必须使用 Phase 05 产出的稳定字段。

### the agent's Discretion
- 新账单分类字段的具体命名
- `owner_expenses` 是否第一版拆独立 repository 或复用现有维修 repository 后再抽离
- 旧账单兼容字段的具体 fallback 规则
- 安全删除租约的按钮位置、提示文案和二次确认样式
- 水电抄表表单的具体视觉布局

</decisions>

<specifics>
## 具体想法

- 用户当前有大几十套房要出租，真实使用中不可避免有灯、墙壁、厕所堵塞等维修，也会请人保洁或打理，这些费用都是房东承担。
- 维修和打理录入的目的不是向租客收钱，而是留痕、复盘哪些房间问题多，并支持后续导出。
- 消防押金和锁卡押金是明确要新增的固定费用，均为一次性押金类。
- 自定义费用必须谨慎，因为后续账单、导出、收据和提醒都会依赖这些性质。
- 租约录错要能补救，但删除必须有安全边界；已经有收款、收据、维修/支出的租约不能硬删。

</specifics>

<canonical_refs>
## 规范参考

下游 agent 在规划或实现本阶段前必须先阅读这些文件。

### 项目与阶段约束
- `.planning/PROJECT.md` — 当前项目目标、已删除范围、费用/支出/导出关键决策
- `.planning/REQUIREMENTS.md` — `FEE-*`、`UTIL-*`、`OPEX-*`、`CORR-*` 需求定义
- `.planning/ROADMAP.md` — 第 5 阶段目标、计划拆分和成功标准
- `.planning/STATE.md` — 当前项目焦点及讨论中锁定的阶段决策

### 数据集合约束
- `.planning/DATA-COLLECTION-MATRIX.md` — Phase 05/06 目标集合职责、写入口、强约束和反模式

### 前序阶段决策
- `.planning/phases/02-账单与房态视图/2-CONTEXT.md` — 账单事实、收款状态、详情页账单心智
- `.planning/phases/03-首页概览与提醒引擎/3-CONTEXT.md` — 提醒、异常、建议的统一口径
- `.planning/phases/04-维修档案与异常分析/4-CONTEXT.md` — 维修事实、维修统计和高频异常口径

### 代码事实来源
- `cloudfunctions/shared/repositories/bill-repository.ts` — 当前账单生成、同步、收款和手工补录逻辑
- `cloudfunctions/shared/schemas/bill.ts` — 当前账单 schema
- `cloudfunctions/shared/schemas/lease.ts` — 当前租约费用规则 schema
- `cloudfunctions/shared/repositories/lease-repository.ts` — 当前租约创建、编辑、结束与账单同步入口
- `cloudfunctions/shared/repositories/repair-record-repository.ts` — 当前维修记录和维修统计逻辑
- `cloudfunctions/shared/repositories/alert-repository.ts` — 当前 `alerts` 重建逻辑

</canonical_refs>

<code_context>
## 现有代码洞察

### 可复用资产
- `cloudfunctions/shared/repositories/bill-repository.ts` 已有系统账单生成、收款、手工补录入口，是 Phase 05 的主要改造点。
- `cloudfunctions/shared/schemas/lease.ts` 已有 `feeRules` 和 `customFeeItems`，可以扩展费用性质，而不是完全重建租约模型。
- `cloudfunctions/shared/repositories/repair-record-repository.ts` 已有维修记录、租住期间关联和统计能力，可承接维修类问题分析。
- `miniprogram/pages/unit-detail/index.ts` 已有月度账单、补录费用、记录维修、续租和结束租约入口，是前端落点。

### 已建立模式
- 页面层通过 service 调云函数，不直接读写集合。
- 详情页和列表页消费后端聚合结果，不在前端自行推导经营真相。
- 云函数 shared 副本需要保持可独立部署，涉及 shared 改动时必须同步传播。

### 集成点
- `leases-save` 触发系统账单同步，需要改成保留已收/手工账单。
- `bills-save` 需要承接水电抄表、其他租客应收和补录删除/作废。
- `repair-record-save` 或新增 `owner-expense-save` 需要承接房东支出。
- `rentable-unit-detail` 需要展示新账单分类、水电读数和房东支出摘要，但不得在查询时写真相集合。

</code_context>

<deferred>
## 延后想法

- 月度导出和收据生成 — 第 6 阶段
- Excel 批量导入 — 已从当前路线删除
- 微信官方提醒消息实际发送闭环 — 已从当前路线删除
- 维修照片/附件上传 — 未来附件能力
- 租客端报修或租客自助 — 未来租客侧能力

</deferred>

---
*阶段：05-费用与账单模型优化*  
*上下文收集时间：2026-04-27*
