---
phase: "06-月度导出与收据"
plan: "06-01"
title: "导出与收据领域合同、测试基座和数据边界"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/mock-cloud.ts
  - tests/cloud/report-export-create.spec.ts
  - tests/cloud/receipt-create.spec.ts
  - tests/cloud/receipt-void.spec.ts
  - cloudfunctions/shared/constants/collections.ts
  - cloudfunctions/shared/schemas/receipt.ts
  - cloudfunctions/shared/schemas/report-export.ts
  - cloudfunctions/shared/repositories/receipt-repository.ts
  - cloudfunctions/shared/repositories/report-export-repository.ts
requirements: [EXPT-01, EXPT-02, EXPT-03, RCPT-01, RCPT-02]
requirements_addressed: [EXPT-01, EXPT-02, EXPT-03, RCPT-01, RCPT-02]
autonomous: true
must_haves:
  truths:
    - "月度导出按 landlordOpenId、月份和可选房源/房间范围过滤"
    - "导出聚合使用 Phase 05 账单字段，不靠 label 猜测费用性质"
    - "收据只针对已收租客账单生成，不包含 owner_expenses"
    - "收据保存完整快照，后续账单更正不能改写旧收据"
  artifacts:
    - path: "cloudfunctions/shared/schemas/receipt.ts"
      provides: "收据快照 schema"
    - path: "cloudfunctions/shared/schemas/report-export.ts"
      provides: "导出请求、sheet 行和导出元数据 schema"
    - path: "tests/cloud/report-export-create.spec.ts"
      provides: "导出口径测试基座"
    - path: "tests/cloud/receipt-create.spec.ts"
      provides: "收据生成测试基座"
---

<objective>
建立 Phase 06 的领域合同和测试基座：定义导出聚合、Excel sheet 行、收据快照、作废状态、重复生成保护和 mock 能力，让后续云函数实现有稳定验收目标。

Purpose: 覆盖 `EXPT-01`、`EXPT-02`、`EXPT-03`、`RCPT-01`、`RCPT-02` 的数据边界。
Output: 共享 schema/repository 骨架、测试文件和 mock 支撑。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DATA-COLLECTION-MATRIX.md
@.planning/phases/06-月度导出与收据/6-CONTEXT.md
@.planning/phases/06-月度导出与收据/06-RESEARCH.md
@.planning/phases/06-月度导出与收据/06-VALIDATION.md
@cloudfunctions/shared/schemas/bill.ts
@cloudfunctions/shared/schemas/owner-expense.ts
@cloudfunctions/shared/constants/collections.ts
@tests/helpers/mock-cloud.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>06-01-01</id>
  <title>补导出口径测试和 report export 合同</title>
  <name>Task 1: 补导出口径测试和 report export 合同</name>
  <files>tests/helpers/mock-cloud.ts, tests/cloud/report-export-create.spec.ts, cloudfunctions/shared/constants/collections.ts, cloudfunctions/shared/schemas/report-export.ts, cloudfunctions/shared/repositories/report-export-repository.ts</files>
  <read_first>
    - .planning/phases/06-月度导出与收据/6-CONTEXT.md
    - .planning/phases/06-月度导出与收据/06-VALIDATION.md
    - cloudfunctions/shared/schemas/bill.ts
    - cloudfunctions/shared/schemas/owner-expense.ts
    - tests/helpers/mock-cloud.ts
  </read_first>
  <action>
    先写失败测试，再补最小领域骨架：
    - 新建 `tests/cloud/report-export-create.spec.ts`，准备同一房东两个房源/房间、另一个房东的干扰数据、已收/未收账单、水电抄表账单、押金类账单和 `owner_expenses`。
    - 测试名必须包含 `builds required workbook sheets for selected month`、`filters export by asset and room`、`keeps owner expenses out of tenant income total`、`includes meter readings in monthly detail rows`。
    - `tests/helpers/mock-cloud.ts` 如需要，补 `report_exports` 集合和云存储 `uploadFile` mock；不要破坏已有测试。
    - `COLLECTIONS` 增加 `reportExports: 'report_exports'`。
    - 新建 `cloudfunctions/shared/schemas/report-export.ts`，定义 `month`、`assetId`、`roomId`、`fileID`、`fileName`、`sheetNames`、`summary`、`createdAt` 等结构。
    - 新建 `report-export-repository.ts`，先提供可测试的纯函数骨架：范围过滤、月度明细行构造、租客收入合计、房东支出汇总、sheet 数据组装。
  </action>
  <acceptance_criteria>
    - `test -f tests/cloud/report-export-create.spec.ts` 成功。
    - `rg -n "builds required workbook sheets for selected month|filters export by asset and room|keeps owner expenses out of tenant income total|includes meter readings in monthly detail rows" tests/cloud/report-export-create.spec.ts` 能找到测试名。
    - `rg -n "reportExports: 'report_exports'" cloudfunctions/shared/constants/collections.ts` 能找到集合常量。
    - `test -f cloudfunctions/shared/schemas/report-export.ts` 成功。
    - `test -f cloudfunctions/shared/repositories/report-export-repository.ts` 成功。
  </acceptance_criteria>
  <done>导出测试基座、集合常量、schema 和 repository 骨架均已就位。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/report-export-create.spec.ts --runInBand</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>06-01-02</id>
  <title>补收据快照测试和 receipt 合同</title>
  <name>Task 2: 补收据快照测试和 receipt 合同</name>
  <files>tests/cloud/receipt-create.spec.ts, tests/cloud/receipt-void.spec.ts, cloudfunctions/shared/schemas/receipt.ts, cloudfunctions/shared/repositories/receipt-repository.ts</files>
  <read_first>
    - .planning/phases/06-月度导出与收据/6-CONTEXT.md
    - .planning/DATA-COLLECTION-MATRIX.md
    - cloudfunctions/shared/schemas/bill.ts
    - tests/helpers/mock-cloud.ts
    - tests/cloud/leases-delete.spec.ts
  </read_first>
  <action>
    建立收据快照与作废测试：
    - 新建 `tests/cloud/receipt-create.spec.ts`，测试名包含 `creates snapshot only from paid tenant bills`、`rejects unpaid bills and owner expenses`、`links created receipt back to bills`、`does not mutate receipt snapshot when bill changes`。
    - 新建 `tests/cloud/receipt-void.spec.ts`，测试名包含 `voids receipt without deleting snapshot`、`allows reissue from voided receipt`。
    - 新建 `cloudfunctions/shared/schemas/receipt.ts`，字段至少包括：`id`、`receiptNo`、`landlordOpenId`、`leaseId`、`roomId`、`tenantId`、`billIds`、`title`、`items`、`totalAmount`、`receivedAt`、`collectorName`、`note`、`status`、`voidedAt`、`voidReason`、`reissueFromReceiptId`、`createdAt`、`updatedAt`。
    - 新建 `receipt-repository.ts`，先实现或占位导出纯函数：校验可开收据账单、构建快照、生成编号、作废快照；后续计划接云函数。
  </action>
  <acceptance_criteria>
    - `test -f tests/cloud/receipt-create.spec.ts` 成功。
    - `test -f tests/cloud/receipt-void.spec.ts` 成功。
    - `rg -n "creates snapshot only from paid tenant bills|rejects unpaid bills and owner expenses|links created receipt back to bills|does not mutate receipt snapshot when bill changes" tests/cloud/receipt-create.spec.ts` 能找到测试名。
    - `rg -n "voids receipt without deleting snapshot|allows reissue from voided receipt" tests/cloud/receipt-void.spec.ts` 能找到测试名。
    - `test -f cloudfunctions/shared/schemas/receipt.ts` 成功。
    - `test -f cloudfunctions/shared/repositories/receipt-repository.ts` 成功。
  </acceptance_criteria>
  <done>收据快照、作废和重开测试基座已就位。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/receipt-void.spec.ts --runInBand</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/report-export-create.spec.ts tests/cloud/receipt-create.spec.ts tests/cloud/receipt-void.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- Phase 06 需要的导出和收据真相合同明确存在。
- 测试能锁定跨房东隔离、租客收入合计、房东支出分离、收据快照和作废重开。
- 本计划不实现小程序页面，不生成最终 Excel 文件，只交付后续实现的安全基座。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-月度导出与收据/06-01-SUMMARY.md`。
</output>
