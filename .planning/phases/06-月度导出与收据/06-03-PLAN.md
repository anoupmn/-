---
phase: "06-月度导出与收据"
plan: "06-03"
title: "收据快照、预览数据、作废与重开"
type: execute
wave: 3
depends_on: ["06-01"]
files_modified:
  - tests/cloud/receipt-create.spec.ts
  - tests/cloud/receipt-void.spec.ts
  - tests/cloud/leases-delete.spec.ts
  - cloudfunctions/shared/repositories/receipt-repository.ts
  - cloudfunctions/shared/schemas/receipt.ts
  - cloudfunctions/shared/repositories/bill-repository.ts
  - cloudfunctions/receipt-create/index.ts
  - cloudfunctions/receipt-create/package.json
  - cloudfunctions/receipt-void/index.ts
  - cloudfunctions/receipt-void/package.json
  - miniprogram/services/receipt.ts
requirements: [RCPT-01, RCPT-02]
requirements_addressed: [RCPT-01, RCPT-02]
autonomous: true
must_haves:
  truths:
    - "收据只针对已收租客账单"
    - "收据固定标注“收款收据（非发票）”"
    - "收据生成后内容快照固定"
    - "收据录错只能作废重开，旧收据不可覆盖"
  artifacts:
    - path: "cloudfunctions/receipt-create/index.ts"
      provides: "生成收据云函数"
    - path: "cloudfunctions/receipt-void/index.ts"
      provides: "作废收据云函数"
    - path: "cloudfunctions/shared/repositories/receipt-repository.ts"
      provides: "收据快照、编号和作废规则"
    - path: "miniprogram/services/receipt.ts"
      provides: "小程序收据服务"
---

<objective>
实现收据领域闭环：从已收租客账单生成固定快照，关联账单 `receiptId`，支持预览数据读取、作废和重开。

Purpose: 覆盖 `RCPT-01`、`RCPT-02`。
Output: `receipt-create`、`receipt-void` 云函数、共享 repository 和小程序服务。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-月度导出与收据/6-CONTEXT.md
@.planning/phases/06-月度导出与收据/06-RESEARCH.md
@.planning/phases/06-月度导出与收据/06-VALIDATION.md
@cloudfunctions/shared/repositories/receipt-repository.ts
@cloudfunctions/shared/repositories/bill-repository.ts
@cloudfunctions/shared/schemas/bill.ts
@cloudfunctions/shared/schemas/receipt.ts
@tests/cloud/receipt-create.spec.ts
@tests/cloud/receipt-void.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>06-03-01</id>
  <title>实现收据创建和账单反向引用</title>
  <name>Task 1: 实现收据创建和账单反向引用</name>
  <files>tests/cloud/receipt-create.spec.ts, cloudfunctions/shared/repositories/receipt-repository.ts, cloudfunctions/shared/repositories/bill-repository.ts, cloudfunctions/receipt-create/index.ts, cloudfunctions/receipt-create/package.json, miniprogram/services/receipt.ts</files>
  <read_first>
    - .planning/phases/06-月度导出与收据/6-CONTEXT.md
    - cloudfunctions/shared/repositories/receipt-repository.ts
    - cloudfunctions/shared/repositories/bill-repository.ts
    - tests/cloud/receipt-create.spec.ts
  </read_first>
  <action>
    完成收据生成：
    - `receipt-create` 支持 `billIds`，以及可选 `month + roomId` 选择该房间当月已收账单。
    - 校验所有账单属于当前房东、`responsibility === 'tenant'`、`receivedAt` 存在、`receivedAmount !== null`、未作废、未绑定有效收据。
    - 禁止把 `owner_expenses` 或未收账单纳入收据；错误信息应明确可读。
    - 快照项目保存费用类型、费用性质、项目名称、应收日期、应收金额、实收日期、实收金额、水电读数和备注。
    - 保存 `title: "收款收据（非发票）"`、收据编号、房源/房间/租客快照、合计金额、收款日期、收款人、备注、生成时间。
    - 创建收据后把相关 `bills` 写入 `receiptId` 和 `receiptNo`，供删除保护和重复开票保护使用。
    - 新增 `miniprogram/services/receipt.ts`，封装 `createReceipt(payload)`。
  </action>
  <acceptance_criteria>
    - `test -f cloudfunctions/receipt-create/index.ts` 成功。
    - `rg -n "收款收据（非发票）|receiptId|receiptNo|createReceipt" cloudfunctions/shared/repositories/receipt-repository.ts cloudfunctions/receipt-create/index.ts miniprogram/services/receipt.ts` 能找到核心逻辑。
    - `npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts --runInBand` 通过。
  </acceptance_criteria>
  <done>已收租客账单可生成固定收据快照，并反写账单收据引用。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/leases-delete.spec.ts --runInBand</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>06-03-02</id>
  <title>实现收据作废和重开保护</title>
  <name>Task 2: 实现收据作废和重开保护</name>
  <files>tests/cloud/receipt-void.spec.ts, tests/cloud/receipt-create.spec.ts, cloudfunctions/shared/repositories/receipt-repository.ts, cloudfunctions/receipt-void/index.ts, cloudfunctions/receipt-void/package.json, miniprogram/services/receipt.ts</files>
  <read_first>
    - .planning/phases/06-月度导出与收据/6-CONTEXT.md
    - cloudfunctions/shared/repositories/receipt-repository.ts
    - tests/cloud/receipt-void.spec.ts
    - tests/cloud/receipt-create.spec.ts
  </read_first>
  <action>
    完成作废与重开语义：
    - 新增 `receipt-void` 云函数，输入 `receiptId`、`voidReason`，只允许当前房东作废自己的有效收据。
    - 作废时保留旧收据所有快照字段，只更新 `status: 'voided'`、`voidedAt`、`voidReason`、`updatedAt`。
    - 旧账单历史不删除；是否清理账单 `receiptId` 由 repository 明确处理。推荐保留历史引用，并在重开时允许 `reissueFromReceiptId` 指向已作废收据。
    - `receipt-create` 支持 `reissueFromReceiptId`，要求原收据已作废，新收据生成新编号并记录来源。
    - 服务层补 `voidReceipt(payload)`。
  </action>
  <acceptance_criteria>
    - `test -f cloudfunctions/receipt-void/index.ts` 成功。
    - `rg -n "voided|voidReason|reissueFromReceiptId|voidReceipt" cloudfunctions/shared/repositories/receipt-repository.ts cloudfunctions/receipt-void/index.ts miniprogram/services/receipt.ts` 能找到作废和重开逻辑。
    - `npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/receipt-void.spec.ts tests/cloud/leases-delete.spec.ts --runInBand` 通过。
  </acceptance_criteria>
  <done>收据可作废、可重开，旧快照不被覆盖，删除保护仍识别历史收据。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/receipt-void.spec.ts tests/cloud/leases-delete.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/receipt-void.spec.ts tests/cloud/leases-delete.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- 收据只从已收租客账单生成。
- 收据快照包含项目、合计、收款日期、收款人、生成时间和“非发票”标识。
- 账单后续修改不改变已生成收据。
- 录错时只能作废重开，旧收据保留可追溯。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-月度导出与收据/06-03-SUMMARY.md`。
</output>
