---
phase: "07-收据管理与凭证体验补全"
plan: "07-02"
title: "收据记录管理页、筛选和业务维护入口"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/cloud/unit-detail-flow.spec.ts
  - miniprogram/app.json
  - miniprogram/services/asset.ts
  - miniprogram/services/room.ts
  - miniprogram/services/tenant.ts
  - miniprogram/services/receipt.ts
  - miniprogram/pages/ops/index.ts
  - miniprogram/pages/ops/index.wxml
  - miniprogram/pages/ops/index.wxss
  - miniprogram/pages/receipt-records/index.ts
  - miniprogram/pages/receipt-records/index.wxml
  - miniprogram/pages/receipt-records/index.wxss
  - miniprogram/pages/receipt-records/index.json
requirements: [RCPT-03, RCPT-04, RCPT-08]
requirements_addressed: [RCPT-03, RCPT-04, RCPT-08]
autonomous: true
must_haves:
  truths:
    - "收据管理入口位于业务维护，不放在首页"
    - "用户用月份、房源、房间、租客和状态筛选收据，不输入内部 ID"
    - "列表卡片展示收据编号、房源/房间、租客、收款日期、金额和作废/重开状态"
    - "页面内部可以使用 receiptId 跳转，但界面不得展示 receiptId、billIds、assetId、roomId 或 tenantId"
  artifacts:
    - path: "miniprogram/pages/receipt-records/index.ts"
      provides: "收据记录管理页逻辑"
    - path: "miniprogram/pages/receipt-records/index.wxml"
      provides: "收据筛选和列表界面"
    - path: "miniprogram/pages/ops/index.wxml"
      provides: "业务维护内的收据记录入口"
    - path: "tests/cloud/unit-detail-flow.spec.ts"
      provides: "页面注册和入口文案静态测试"
---

<objective>
把后端收据列表接成个人房东可用的收据记录管理中心：从业务维护进入，按业务标签筛选，打开收据预览或回到房间详情。

Purpose: 覆盖 `RCPT-03`、`RCPT-04`、`RCPT-08` 的小程序管理体验。
Output: `pages/receipt-records`、业务维护入口、页面注册、服务调用和静态/集成测试。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-收据管理与凭证体验补全/7-CONTEXT.md
@.planning/phases/07-收据管理与凭证体验补全/07-RESEARCH.md
@.planning/phases/07-收据管理与凭证体验补全/07-VALIDATION.md
@miniprogram/app.json
@miniprogram/pages/ops/index.ts
@miniprogram/pages/ops/index.wxml
@miniprogram/pages/report-export/index.ts
@miniprogram/pages/report-export/index.wxml
@miniprogram/pages/unit-detail/index.ts
@miniprogram/services/receipt.ts
@tests/cloud/unit-detail-flow.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>07-02-01</id>
  <title>注册收据记录页并接入业务维护入口</title>
  <name>Task 1: 注册收据记录页并接入业务维护入口</name>
  <files>tests/cloud/unit-detail-flow.spec.ts, miniprogram/app.json, miniprogram/pages/ops/index.ts, miniprogram/pages/ops/index.wxml, miniprogram/pages/ops/index.wxss, miniprogram/pages/receipt-records/index.ts, miniprogram/pages/receipt-records/index.wxml, miniprogram/pages/receipt-records/index.wxss, miniprogram/pages/receipt-records/index.json, miniprogram/services/receipt.ts</files>
  <read_first>
    - .planning/phases/07-收据管理与凭证体验补全/7-CONTEXT.md
    - miniprogram/app.json
    - miniprogram/pages/ops/index.ts
    - miniprogram/pages/ops/index.wxml
    - miniprogram/pages/report-export/index.ts
    - miniprogram/pages/report-export/index.wxml
    - miniprogram/services/receipt.ts
    - tests/cloud/unit-detail-flow.spec.ts
  </read_first>
  <action>
    建立收据记录页面入口：
    - `miniprogram/app.json` 增加页面路径 `pages/receipt-records/index`。
    - 在 `pages/ops` 的经营凭证区域增加入口文案 `收据记录`，跳转路径为 `/pages/receipt-records/index`；保持业务维护页现有朴素工具型风格。
    - 新建 `pages/receipt-records/index.json`，标题使用 `收据记录`。
    - 新建 `pages/receipt-records/index.ts`，页面初始数据包含 `month`、`assetId`、`roomId`、`tenantId`、`status`、`receipts`、`loading`、`error`、`assetOptions`、`roomOptions`、`tenantOptions`、`statusOptions`。
    - 页面 `onLoad` 调用 `listReceiptRecords` 或 `listReceipts` 加载数据；筛选变化后重新调用 `receipt-list`。
    - 新建 `pages/receipt-records/index.wxml` 和 `index.wxss`，第一屏必须包含月份选择、房源选择、房间选择、租客选择、状态选择、收据卡片列表和空状态。
    - `tests/cloud/unit-detail-flow.spec.ts` 增加静态断言：`app.json` 含 `pages/receipt-records/index`，`ops` 页面含 `收据记录` 和 `/pages/receipt-records/index`。
  </action>
  <acceptance_criteria>
    - `rg -n "pages/receipt-records/index" miniprogram/app.json tests/cloud/unit-detail-flow.spec.ts` 能找到页面注册断言。
    - `rg -n "收据记录|/pages/receipt-records/index" miniprogram/pages/ops/index.wxml tests/cloud/unit-detail-flow.spec.ts` 能找到业务维护入口。
    - `rg -n "listReceiptRecords|listReceipts|receipt-list|month|assetId|roomId|tenantId|status" miniprogram/pages/receipt-records/index.ts miniprogram/services/receipt.ts` 能找到服务调用和筛选字段。
    - `npm test -- --runTestsByPath tests/cloud/unit-detail-flow.spec.ts tests/cloud/receipt-list.spec.ts --runInBand` 通过。
    - `npm run typecheck` 通过。
  </acceptance_criteria>
  <done>用户可以从业务维护进入收据记录管理页并加载收据列表。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/unit-detail-flow.spec.ts tests/cloud/receipt-list.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>07-02-02</id>
  <title>完成收据列表筛选、卡片操作和详情闭环</title>
  <name>Task 2: 完成收据列表筛选、卡片操作和详情闭环</name>
  <files>tests/cloud/unit-detail-flow.spec.ts, miniprogram/pages/receipt-records/index.ts, miniprogram/pages/receipt-records/index.wxml, miniprogram/pages/receipt-records/index.wxss, miniprogram/pages/receipt/index.ts, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/services/asset.ts, miniprogram/services/room.ts, miniprogram/services/tenant.ts, miniprogram/services/receipt.ts</files>
  <read_first>
    - miniprogram/pages/receipt-records/index.ts
    - miniprogram/pages/receipt-records/index.wxml
    - miniprogram/pages/report-export/index.ts
    - miniprogram/pages/report-export/index.wxml
    - miniprogram/pages/receipt/index.ts
    - miniprogram/pages/unit-detail/index.ts
    - miniprogram/services/asset.ts
    - miniprogram/services/room.ts
    - miniprogram/services/tenant.ts
    - miniprogram/services/receipt.ts
    - tests/cloud/unit-detail-flow.spec.ts
  </read_first>
  <action>
    完善管理页可用性：
    - 月份筛选默认当前月份，支持用户通过 picker 切换 `YYYY-MM`；清空/全部选项保留为 `全部月份`。
    - 房源、房间、租客筛选使用服务读取业务对象并生成中文 label；用户界面只展示名称，不展示 `assetId`、`roomId`、`tenantId`。
    - 状态筛选提供 `全部`、`有效`、`已作废` 三项，传给后端的值分别为 `all`、`active`、`voided`。
    - 列表卡片展示 `receiptNo`、`assetName / roomName`、`tenantName`、`receivedAt`、`totalAmount`、`billCount`、状态文案 `有效` 或 `已作废`；如有 `reissueFromReceiptId`，展示 `重开收据` 或 `由作废收据重开`，不要展示来源 ID。
    - 卡片操作提供 `查看收据` 跳转 `/pages/receipt/index?receiptId=${id}`，提供 `回到房间` 跳转 `/pages/unit-detail/index?roomId=${roomId}`；按钮文案不得包含内部 ID。
    - 空状态文案使用 `暂无符合条件的收据`；错误态展示云函数错误的用户可读摘要。
    - `tests/cloud/unit-detail-flow.spec.ts` 增加静态断言：记录页含 `全部`、`有效`、`已作废`、`查看收据`、`回到房间`、`暂无符合条件的收据`，且 WXML 不包含 `receiptId:`、`billIds` 这样的可见标签。
  </action>
  <acceptance_criteria>
    - `rg -n "全部月份|全部|有效|已作废|查看收据|回到房间|暂无符合条件的收据" miniprogram/pages/receipt-records/index.wxml miniprogram/pages/receipt-records/index.ts tests/cloud/unit-detail-flow.spec.ts` 能找到筛选、操作和空状态文案。
    - `rg -n "receiptId:|billIds|assetId:|roomId:|tenantId:" miniprogram/pages/receipt-records/index.wxml` 没有匹配。
    - `rg -n "/pages/receipt/index\\?receiptId=|/pages/unit-detail/index\\?roomId=" miniprogram/pages/receipt-records/index.ts` 能找到跳转路径。
    - `npm test -- --runTestsByPath tests/cloud/unit-detail-flow.spec.ts tests/cloud/receipt-list.spec.ts --runInBand` 通过。
    - `npm run typecheck` 通过。
  </acceptance_criteria>
  <done>收据记录页能筛选、查看收据、回到房间详情，并避免暴露内部 ID。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/unit-detail-flow.spec.ts tests/cloud/receipt-list.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/unit-detail-flow.spec.ts tests/cloud/receipt-list.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- 业务维护页有清晰的收据记录入口。
- 收据记录页可以按月份、房源、房间、租客和状态筛选。
- 每张收据能展示关联业务信息、收款日期、金额和状态。
- 用户可以从记录页进入收据预览或回到房间详情。
- 界面不暴露内部 ID。
</success_criteria>

<output>
完成后创建 `.planning/phases/07-收据管理与凭证体验补全/07-02-SUMMARY.md`。
</output>
