---
phase: "07-收据管理与凭证体验补全"
plan: "07-03"
title: "合并开具、作废原因输入和重开追溯体验"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/cloud/receipt-create.spec.ts
  - tests/cloud/receipt-void.spec.ts
  - tests/cloud/unit-detail-flow.spec.ts
  - cloudfunctions/shared/repositories/receipt-repository.ts
  - cloudfunctions/receipt-create/index.ts
  - cloudfunctions/receipt-void/index.ts
  - miniprogram/pages/unit-detail/index.ts
  - miniprogram/pages/unit-detail/index.wxml
  - miniprogram/pages/unit-detail/index.wxss
  - miniprogram/pages/receipt/index.ts
  - miniprogram/pages/receipt/index.wxml
  - miniprogram/pages/receipt/index.wxss
  - miniprogram/services/receipt.ts
requirements: [RCPT-05, RCPT-06, RCPT-08]
requirements_addressed: [RCPT-05, RCPT-06, RCPT-08]
autonomous: true
must_haves:
  truths:
    - "一个房间一个月份的多笔已收租客账单可以合并生成一张收据"
    - "已存在有效收据的账单不得再次被选择或生成有效收据"
    - "作废弹窗必须让用户填写原因，不能继续使用硬编码原因"
    - "作废旧收据保留快照，新收据通过 reissueFromReceiptId 追溯来源"
    - "房间详情、收据预览和收据记录之间使用业务文案形成闭环，不展示内部 ID"
  artifacts:
    - path: "miniprogram/pages/unit-detail/index.ts"
      provides: "合并开具候选账单和入口"
    - path: "miniprogram/pages/receipt/index.ts"
      provides: "作废原因输入和重开追溯"
    - path: "tests/cloud/receipt-create.spec.ts"
      provides: "合并开具和重复生成后端测试"
    - path: "tests/cloud/receipt-void.spec.ts"
      provides: "作废原因后端测试"
---

<objective>
把 Phase 06 的单张收据功能补齐为可日常使用的合并开具、作废原因和重开追溯体验。

Purpose: 覆盖 `RCPT-05`、`RCPT-06`、`RCPT-08`。
Output: 房间详情合并开具入口、收据页作废原因输入、重开来源提示和相关测试。
</objective>

<execution_context>
@/Users/zengzhipeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zengzhipeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-收据管理与凭证体验补全/7-CONTEXT.md
@.planning/phases/07-收据管理与凭证体验补全/07-RESEARCH.md
@.planning/phases/07-收据管理与凭证体验补全/07-VALIDATION.md
@cloudfunctions/shared/repositories/receipt-repository.ts
@cloudfunctions/shared/schemas/receipt.ts
@cloudfunctions/shared/schemas/bill.ts
@miniprogram/pages/unit-detail/index.ts
@miniprogram/pages/unit-detail/index.wxml
@miniprogram/pages/receipt/index.ts
@miniprogram/pages/receipt/index.wxml
@miniprogram/services/receipt.ts
@tests/cloud/receipt-create.spec.ts
@tests/cloud/receipt-void.spec.ts
@tests/cloud/unit-detail-flow.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <id>07-03-01</id>
  <title>在房间详情接入月度合并开具收据</title>
  <name>Task 1: 在房间详情接入月度合并开具收据</name>
  <files>tests/cloud/receipt-create.spec.ts, tests/cloud/unit-detail-flow.spec.ts, cloudfunctions/shared/repositories/receipt-repository.ts, cloudfunctions/receipt-create/index.ts, miniprogram/pages/unit-detail/index.ts, miniprogram/pages/unit-detail/index.wxml, miniprogram/pages/unit-detail/index.wxss, miniprogram/services/receipt.ts</files>
  <read_first>
    - .planning/phases/07-收据管理与凭证体验补全/7-CONTEXT.md
    - .planning/phases/07-收据管理与凭证体验补全/07-RESEARCH.md
    - cloudfunctions/shared/repositories/receipt-repository.ts
    - miniprogram/pages/unit-detail/index.ts
    - miniprogram/pages/unit-detail/index.wxml
    - miniprogram/services/receipt.ts
    - tests/cloud/receipt-create.spec.ts
    - tests/cloud/unit-detail-flow.spec.ts
  </read_first>
  <action>
    实现房间详情合并开具：
    - 复用 `07-01` 的服务端校验，确保 `receipt-create` 支持 `month + roomId` 生成同房间、同租客/租约、同月份的多笔已收租客账单。
    - 房间详情月度账单区域新增合并入口文案 `合并开具本月收据`，只在当前月份存在 2 笔及以上可开收据的已收租客账单时启用。
    - 可开收据账单定义为：`responsibility === 'tenant'`、有 `receivedAt`、有 `receivedAmount`、没有有效收据；已有有效收据的账单显示 `已开具收据` 或 `查看收据`，不得进入可选候选。
    - 前端展示可纳入收据的账单项：费用名称、应收日期、实收日期、实收金额；不展示 `billId`。
    - 用户确认后调用 `createReceipt({ roomId, month })` 或明确的 `billIds`；推荐 `month + roomId`，由后端最终裁决可纳入账单。
    - 创建成功后跳转 `/pages/receipt/index?receiptId=${receipt.id}`，提示文案使用 `收据已生成` 和收据编号。
    - `tests/cloud/unit-detail-flow.spec.ts` 增加静态断言：详情页含 `合并开具本月收据`、`已开具收据`、`收据已生成`，且 WXML 不渲染 `billId` 标签。
  </action>
  <acceptance_criteria>
    - `rg -n "合并开具本月收据|已开具收据|收据已生成" miniprogram/pages/unit-detail/index.ts miniprogram/pages/unit-detail/index.wxml tests/cloud/unit-detail-flow.spec.ts` 能找到合并开具文案。
    - `rg -n "month.*roomId|roomId.*month|createReceipt" miniprogram/pages/unit-detail/index.ts cloudfunctions/receipt-create/index.ts cloudfunctions/shared/repositories/receipt-repository.ts` 能找到合并创建调用。
    - `rg -n "billId:" miniprogram/pages/unit-detail/index.wxml` 没有匹配。
    - `npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand` 通过。
    - `npm run typecheck` 通过。
  </acceptance_criteria>
  <done>房间详情可以针对同月多笔已收租客账单合并生成一张收据，并防止重复纳入。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <id>07-03-02</id>
  <title>实现作废原因输入、旧收据展示和重开来源提示</title>
  <name>Task 2: 实现作废原因输入、旧收据展示和重开来源提示</name>
  <files>tests/cloud/receipt-void.spec.ts, tests/cloud/unit-detail-flow.spec.ts, cloudfunctions/shared/repositories/receipt-repository.ts, cloudfunctions/receipt-void/index.ts, miniprogram/pages/receipt/index.ts, miniprogram/pages/receipt/index.wxml, miniprogram/pages/receipt/index.wxss, miniprogram/services/receipt.ts</files>
  <read_first>
    - .planning/phases/07-收据管理与凭证体验补全/7-CONTEXT.md
    - .planning/phases/07-收据管理与凭证体验补全/07-RESEARCH.md
    - cloudfunctions/shared/repositories/receipt-repository.ts
    - miniprogram/pages/receipt/index.ts
    - miniprogram/pages/receipt/index.wxml
    - miniprogram/pages/receipt/index.wxss
    - miniprogram/services/receipt.ts
    - tests/cloud/receipt-void.spec.ts
    - tests/cloud/unit-detail-flow.spec.ts
  </read_first>
  <action>
    替换硬编码作废原因并补齐追溯：
    - 在收据预览页新增自定义作废弹窗，不使用 `wx.showModal` 收集输入；WXML 必须包含 `<textarea>`。
    - 弹窗标题使用 `填写作废原因`，确认按钮文案使用 `确认作废`，空原因时页面提示 `请输入作废原因`，不得调用 `voidReceipt`。
    - 调用 `voidReceipt({ receiptId, voidReason })` 时传入用户填写并 trim 后的原因；移除硬编码 `用户作废重开`。
    - 作废后的旧收据预览保留明细、合计和收款日期，同时显示 `已作废`、`作废原因`、`作废时间`。
    - 重开入口文案使用 `重开收据`；重开成功的新收据通过 `reissueFromReceiptId` 指向旧收据。
    - 新收据如带 `reissueFromReceiptId`，预览中显示 `由作废收据重开` 或 `重开来源`，不得显示旧收据 ID。
    - `tests/cloud/unit-detail-flow.spec.ts` 增加静态断言：收据页含 `填写作废原因`、`textarea`、`请输入作废原因`、`作废原因`、`由作废收据重开`，且不再含硬编码原因 `用户作废重开`。
  </action>
  <acceptance_criteria>
    - `rg -n "填写作废原因|textarea|请输入作废原因|作废原因|作废时间|由作废收据重开|重开收据" miniprogram/pages/receipt/index.ts miniprogram/pages/receipt/index.wxml tests/cloud/unit-detail-flow.spec.ts` 能找到作废和重开 UI。
    - `! rg -n "用户作废重开" miniprogram/pages/receipt/index.ts miniprogram/pages/receipt/index.wxml` 成功。
    - `rg -n "voidReason.*trim|voidReason is required|作废原因不能为空" cloudfunctions/shared/repositories/receipt-repository.ts cloudfunctions/receipt-void/index.ts miniprogram/pages/receipt/index.ts` 能找到前后端作废原因校验。
    - `npm test -- --runTestsByPath tests/cloud/receipt-void.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand` 通过。
    - `npm run typecheck` 通过。
  </acceptance_criteria>
  <done>收据作废必须填写原因，旧收据和新收据之间有可读追溯关系。</done>
  <verify>
    <automated>npm test -- --runTestsByPath tests/cloud/receipt-void.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand && npm run typecheck</automated>
  </verify>
</task>
</tasks>

<verification>
- `npm test -- --runTestsByPath tests/cloud/receipt-create.spec.ts tests/cloud/receipt-void.spec.ts tests/cloud/unit-detail-flow.spec.ts --runInBand`
- `npm run typecheck`
</verification>

<success_criteria>
- 一个房间同月多笔已收租客账单可以合并开具一张收据。
- 已有有效收据的账单不会重复进入候选或被后端重复开具。
- 作废时必须填写作废原因，旧收据快照保留。
- 重开收据可追溯来源，但界面不显示内部 ID。
- 房间详情和收据预览之间形成自然闭环。
</success_criteria>

<output>
完成后创建 `.planning/phases/07-收据管理与凭证体验补全/07-03-SUMMARY.md`。
</output>
