---
phase: "08-退租结算"
plan: "08-01"
title: "扩展账单 schema 与退租结算双周期后端逻辑"
type: execute
wave: 1
autonomous: true
must_haves:
  truths:
    - "billTypeSchema 增加 rent_refund 和 deposit_refund 两种类型"
    - "responsibility 从 literal('tenant') 扩展为 enum(['tenant', 'landlord'])"
    - "endLease() 接收 SettlementOptions，不传时保持向后兼容"
    - "退余下租金按 rentAmount/billingCycleDays × days 计算并生成 refund 账单"
    - "退押金按 depositAmount 生成 refund 账单"
    - "voidFutureSystemBills 物理删除该租约 dueDate > closedAt 的未收系统账单"
    - "leases-end 云函数入口透传 settlement 参数"
    - "所有修改的 JS 镜像文件同步更新"
  artifacts:
    - path: "cloudfunctions/shared/schemas/bill.ts"
      provides: "新增 bill type 和扩展 responsibility"
    - path: "cloudfunctions/shared/repositories/lease-repository.ts"
      provides: "endLease() 结算逻辑"
    - path: "cloudfunctions/leases-end/index.ts"
      provides: "云函数入口扩展"
---

<objective>
扩展账单 schema 并实现 endLease() 的结算能力：void future bills、create rent refund、create deposit refund。所有修改同步更新 JS 编译镜像。
</objective>

<tasks>
<task type="auto">
  <id>08-01-01</id>
  <title>扩展 bill schema</title>
  <name>Task 1: 扩展 bill schema</name>
  <files>cloudfunctions/shared/schemas/bill.ts, cloudfunctions/shared/schemas/bill.js</files>
  <read_first>
    - cloudfunctions/shared/schemas/bill.ts
    - .planning/phases/08-退租结算/08-CONTEXT.md
  </read_first>
  <action>
    修改 `billTypeSchema` 在枚举中新增 `'rent_refund'` 和 `'deposit_refund'`：
    ```
    const billTypeSchema = z.enum([
      'rent', 'deposit', 'management', 'fire_deposit', 'lock_card_deposit',
      'water', 'electricity', 'property', 'misc', 'custom',
      'rent_refund', 'deposit_refund'
    ]);
    ```
    修改 `responsibility` 字段（第54行）：
    ```
    // 现有
    responsibility: z.literal('tenant').catch('tenant').default('tenant'),
    // 改为
    responsibility: z.enum(['tenant', 'landlord']).catch('tenant').default('tenant'),
    ```
    同步更新 `cloudfunctions/shared/schemas/bill.js` 中的 zod 编译镜像。
  </action>
  <acceptance_criteria>
    - `rg "rent_refund" cloudfunctions/shared/schemas/bill.ts` 能找到
    - `rg "deposit_refund" cloudfunctions/shared/schemas/bill.ts` 能找到
    - `rg "z.enum\\(\['tenant', 'landlord'\\]\\)" cloudfunctions/shared/schemas/bill.ts` 能找到
    - `rg "rent_refund" cloudfunctions/shared/schemas/bill.js` 能找到
  </acceptance_criteria>
</task>

<task type="auto">
  <id>08-01-02</id>
  <title>扩展 endLease() 支持结算参数</title>
  <name>Task 2: 扩展 endLease() 支持结算参数</name>
  <files>cloudfunctions/shared/repositories/lease-repository.ts, cloudfunctions/shared/repositories/lease-repository.js, cloudfunctions/leases-end/shared/repositories/lease-repository.ts, cloudfunctions/leases-end/shared/repositories/lease-repository.js</files>
  <read_first>
    - cloudfunctions/shared/repositories/lease-repository.ts
    - cloudfunctions/shared/schemas/lease.ts
    - cloudfunctions/shared/schemas/bill.ts
    - cloudfunctions/shared/repositories/bill-repository.ts
    - .planning/phases/08-退租结算/08-CONTEXT.md
  </read_first>
  <action>
    1. 在 `lease-repository.ts` 顶部 import 区新增：
    ```typescript
    import { billSchema } from '../schemas/bill';
    import { createId, findById, insertRecord } from '../runtime';
    ```
    2. 在文件末尾 `endLease()` 前新增 `SettlementOptions` 接口和辅助函数：
    ```typescript
    interface SettlementOptions {
      voidFutureSystemBills?: boolean;
      rentRefundDays?: number;
      refundDeposit?: boolean;
      refundFireDeposit?: boolean;
      refundLockCardDeposit?: boolean;
    }

    function calculateRentRefund(lease: Lease, days: number): number {
      if (days <= 0) return 0;
      const dailyRate = lease.rentAmount / lease.billingCycleDays;
      return Math.ceil(dailyRate * days * 100) / 100;
    }
    ```
    3. 修改 `endLease()` 签名增加参数：`endLease(db, leaseId, event, settlement?: SettlementOptions)`
    4. 在现有 `endLease()` 逻辑（关闭租约）之后，返回之前，插入结算逻辑：
    ```typescript
    // === 结算逻辑 ===
    const settlementSummary: {
      voidedBillCount: number;
      createdRefundBills: Array<{ type: string; amount: number }>;
    } = { voidedBillCount: 0, createdRefundBills: [] };
    
    if (settlement) {
      // 2. void future unpaid system bills
      if (settlement.voidFutureSystemBills) {
        const futureUnpaidBills = (await listLeaseBillsSafely(db, leaseId)).filter(
          (bill) => bill.dueDate > result.closedLease.closedAt 
            && !bill.receivedAt 
            && bill.receivedAmount == null
            && (bill.source ?? 'system') === 'system'
        );
        for (const bill of futureUnpaidBills) {
          await removeRecordsByQuery(db, COLLECTIONS.bills, { id: bill.id, landlordOpenId: currentLease.landlordOpenId });
        }
        settlementSummary.voidedBillCount = futureUnpaidBills.length;
      }
      
      // 3. rent refund
      if (settlement.rentRefundDays && settlement.rentRefundDays > 0) {
        const refundAmount = calculateRentRefund(currentLease, settlement.rentRefundDays);
        const refundBill = billSchema.parse({
          id: createId('bill'),
          landlordOpenId: currentLease.landlordOpenId,
          leaseId: currentLease.id,
          roomId: currentLease.roomId,
          type: 'rent_refund',
          section: 'rent',
          dueDate: result.closedLease.closedAt,
          amount: refundAmount,
          status: 'pending',
          receivedAt: null,
          receivedAmount: null,
          note: `退余下租金 ${settlement.rentRefundDays}天`,
          source: 'system',
          feeNature: 'one_time',
          responsibility: 'landlord',
          cadence: 'once',
          isDepositLike: false,
          isOneTime: true,
          createdAt: result.closedLease.closedAt,
          updatedAt: result.closedLease.closedAt
        });
        await insertRecord(db, COLLECTIONS.bills, refundBill);
        settlementSummary.createdRefundBills.push({ type: 'rent_refund', amount: refundAmount });
      }
      
      // 4-6. deposit refunds
      const feeRules = getLeaseFeeRules(currentLease);
      const depositItems = [
        { condition: settlement.refundDeposit, amount: feeRules.deposit.amount, note: '退还押金' },
        { condition: settlement.refundFireDeposit, amount: feeRules.fireDeposit.amount, note: '退还消防押金' },
        { condition: settlement.refundLockCardDeposit, amount: feeRules.lockCardDeposit.amount, note: '退还门禁卡押金' }
      ];
      for (const item of depositItems) {
        if (item.condition && item.amount > 0) {
          const refundBill = billSchema.parse({
            id: createId('bill'),
            landlordOpenId: currentLease.landlordOpenId,
            leaseId: currentLease.id,
            roomId: currentLease.roomId,
            type: 'deposit_refund',
            section: 'deposit',
            dueDate: result.closedLease.closedAt,
            amount: item.amount,
            status: 'pending',
            receivedAt: null,
            receivedAmount: null,
            note: item.note,
            source: 'system',
            feeNature: 'deposit',
            responsibility: 'landlord',
            cadence: 'once',
            isDepositLike: true,
            isOneTime: true,
            createdAt: result.closedLease.closedAt,
            updatedAt: result.closedLease.closedAt
          });
          await insertRecord(db, COLLECTIONS.bills, refundBill);
          settlementSummary.createdRefundBills.push({ type: 'deposit_refund', amount: item.amount });
        }
      }
    }
    ```
    5. 修改 `return` 语句增加 `settlementSummary` 字段
    6. 新增的 import（`createId`、`insertRecord`、`removeRecordsByQuery`、`billSchema`、`getLeaseFeeRules`）如果已有则不要重复
    7. 同步更新 `lease-repository.js` 编译版本和 `leases-end/shared/` 中的副本

    注意：`getLeaseFeeRules` 从 `../schemas/lease` 导入（如果尚未导入）
  </action>
  <acceptance_criteria>
    - `rg "SettlementOptions" cloudfunctions/shared/repositories/lease-repository.ts` 能找到
    - `rg "voidFutureSystemBills" cloudfunctions/shared/repositories/lease-repository.ts` 能找到
    - `rg "rentRefundDays" cloudfunctions/shared/repositories/lease-repository.ts` 能找到
    - `rg "settlementSummary" cloudfunctions/shared/repositories/lease-repository.ts` 能找到
    - `rg "rent_refund" cloudfunctions/shared/repositories/lease-repository.ts` 能找到
    - `rg "deposit_refund" cloudfunctions/shared/repositories/lease-repository.ts` 能找到
    - `diff cloudfunctions/shared/repositories/lease-repository.ts cloudfunctions/leases-end/shared/repositories/lease-repository.ts` 无差异
  </acceptance_criteria>
</task>

<task type="auto">
  <id>08-01-03</id>
  <title>更新 leases-end 云函数入口</title>
  <name>Task 3: 更新 leases-end 云函数入口</name>
  <files>cloudfunctions/leases-end/index.ts, cloudfunctions/leases-end/index.js</files>
  <read_first>
    - cloudfunctions/leases-end/index.ts
    - .planning/phases/08-退租结算/08-CONTEXT.md
  </read_first>
  <action>
    修改 `LeaseEndEvent` 接口增加 `settlement` 字段：
    ```typescript
    export interface LeaseEndEvent extends CloudEventBase {
      leaseId: string;
      settlement?: {
        voidFutureSystemBills?: boolean;
        rentRefundDays?: number;
        refundDeposit?: boolean;
        refundFireDeposit?: boolean;
        refundLockCardDeposit?: boolean;
      };
    }
    ```
    修改 `main()` 函数中调用 `endLease` 时传入 `event.settlement`：
    ```typescript
    return endLease(db, event.leaseId, event, event.settlement);
    ```
    同步更新 `cloudfunctions/leases-end/index.js`。
  </action>
  <acceptance_criteria>
    - `rg "settlement\?" cloudfunctions/leases-end/index.ts` 能找到
    - `rg "event\.settlement" cloudfunctions/leases-end/index.ts` 能找到
  </acceptance_criteria>
</task>

<task type="auto">
  <id>08-01-04</id>
  <title>同步 JS 编译镜像</title>
  <name>Task 4: 同步 JS 编译镜像</name>
  <files>cloudfunctions/shared/schemas/bill.js, cloudfunctions/shared/repositories/lease-repository.js, cloudfunctions/leases-end/shared/repositories/lease-repository.js, cloudfunctions/leases-end/shared/schemas/bill.js</files>
  <read_first>
    - cloudfunctions/shared/schemas/bill.ts
    - cloudfunctions/shared/repositories/lease-repository.ts
    - cloudfunctions/leases-end/index.ts
  </read_first>
  <action>
    确保以下 JS 文件与对应的 TS 文件同步更新：
    - `cloudfunctions/shared/schemas/bill.js` — 包含 `rent_refund`、`deposit_refund`、`landlord` responsibility
    - `cloudfunctions/shared/repositories/lease-repository.js` — 包含 `SettlementOptions`、`calculateRentRefund`、`settlementSummary`
    - `cloudfunctions/leases-end/shared/repositories/lease-repository.js` — 与 shared 版本一致
    - `cloudfunctions/leases-end/index.js` — 包含 `settlement` 参数透传
    检查 `leases-end/shared/` 下是否还有 `bill.js` 或 `schemas/bill.js`，如有也需同步。
  </action>
  <acceptance_criteria>
    - `rg "rent_refund" cloudfunctions/shared/schemas/bill.js` 能找到
    - `rg "landlord" cloudfunctions/shared/schemas/bill.js` 能找到
    - `rg "settlementSummary" cloudfunctions/shared/repositories/lease-repository.js` 能找到
    - `rg "rent_refund" cloudfunctions/leases-end/shared/repositories/lease-repository.js` 能找到
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `rg -c "rent_refund" cloudfunctions/shared/schemas/bill.ts cloudfunctions/shared/repositories/lease-repository.ts`
- `rg -c "deposit_refund" cloudfunctions/shared/schemas/bill.ts cloudfunctions/shared/repositories/lease-repository.ts`
- `rg "settlementSummary" cloudfunctions/shared/repositories/lease-repository.ts cloudfunctions/leases-end/index.ts`
</verification>

<success_criteria>
- billTypeSchema 包含 rent_refund 和 deposit_refund
- responsibility 支持 tenant 和 landlord
- endLease() 不传 settlement 时行为不变
- endLease() 传 settlement 时正确处理 void、退租金、退押金
- leases-end 入口透传参数
- JS 镜像同步
</success_criteria>
