---
phase: "08-退租结算"
plan: "08-02"
title: "unit-detail 退租结算引导面板前端改造"
type: execute
wave: 2
depends_on: ["08-01"]
autonomous: true
must_haves:
  truths:
    - "点击结束租约弹出结算面板，不直接结束"
    - "面板展示未收账单、水电费状态、余下租金预估、押金退还选项"
    - "余下租金天数可调整，期满结束时不显示退款区"
    - "确认结算调用 endLease 传入 settlement 参数"
    - "面板样式复用现有 dialog-card 模式"
  artifacts:
    - path: "miniprogram/pages/unit-detail/index.ts"
      provides: "handleEndLease 改造 + 结算面板交互逻辑"
    - path: "miniprogram/pages/unit-detail/index.wxml"
      provides: "结算面板 UI 模板"
    - path: "miniprogram/pages/unit-detail/index.wxss"
      provides: "结算面板样式"
---

<objective>
将 unit-detail 页的"结束租约"按钮改造为退租结算引导面板：未收账单提醒、水电费补录入口、余下租金计算、押金退还勾选，确认后调用扩展后的 endLease API。
</objective>

<tasks>
<task type="auto">
  <id>08-02-01</id>
  <title>改造 handleEndLease 交互流程 + 结算数据逻辑</title>
  <name>Task 1: 改造 handleEndLease + 结算面板 TS 逻辑</name>
  <files>miniprogram/pages/unit-detail/index.ts</files>
  <read_first>
    - miniprogram/pages/unit-detail/index.ts
    - miniprogram/services/lease.ts
    - .planning/phases/08-退租结算/08-CONTEXT.md
  </read_first>
  <action>
    1. 新增 data 字段：
    ```typescript
    settlementDialogVisible: false,
    settlement: {
      unpaidCount: 0,
      unpaidTotal: 0,
      waterLastDate: '',
      electricityLastDate: '',
      rentRefundDays: 0,
      rentRefundEstimate: 0,
      dailyRent: 0,
      isEarlyTermination: false,
      refundDeposit: false,
      refundFireDeposit: false,
      refundLockCardDeposit: false,
      hasDeposit: false,
      hasFireDeposit: false,
      hasLockCardDeposit: false,
      submitting: false
    }
    ```
    2. 改造 `handleEndLease()` — 改为打开结算面板：
    ```typescript
    async handleEndLease() {
      const lease = this.data.detail?.activeLease;
      if (!lease?.id) return;
      
      // 结算面板数据
      const today = getLocalDateKey();
      const endDate = String(lease.endDate || '').slice(0, 10);
      const remainingDays = Math.max(0, Math.floor((new Date(endDate).getTime() - new Date(today).getTime()) / 86400000));
      const billingCycleDays = Number(lease.billingCycleDays || 30);
      const rentAmount = Number(lease.rentAmount || 0);
      const dailyRent = billingCycleDays > 0 ? rentAmount / billingCycleDays : 0;
      const rentRefundEstimate = Math.ceil(remainingDays * dailyRent * 100) / 100;
      
      const feeRules = lease.feeRules || {};
      const depositAmount = Number(feeRules.deposit?.amount ?? lease.depositAmount ?? 0);
      const fireDepositAmount = Number(feeRules.fireDeposit?.amount ?? 0);
      const lockCardDepositAmount = Number(feeRules.lockCardDeposit?.amount ?? 0);
      
      // 未收账单统计
      const unpaidBills = (this.data.detail?.monthlyBillGroups || [])
        .flatMap((g: any) => g.items || [])
        .filter((b: any) => !b.receivedAt || b.receivedAmount == null);
      const unpaidTotal = unpaidBills.reduce((sum: number, b: any) => sum + Number(b.amount || 0), 0);
      
      // 水电费最后记录
      const findLastMeterDate = (type: string) => {
        const allBills = (this.data.detail?.monthlyBillGroups || [])
          .flatMap((g: any) => g.items || [])
          .filter((b: any) => b.type === type)
          .sort((a: any, b: any) => b.dueDate.localeCompare(a.dueDate));
        return allBills.length > 0 ? allBills[0].dueDate || '' : '';
      };
      
      this.setData({
        settlementDialogVisible: true,
        settlement: {
          unpaidCount: unpaidBills.length,
          unpaidTotal: Math.round(unpaidTotal * 100) / 100,
          waterLastDate: findLastMeterDate('water'),
          electricityLastDate: findLastMeterDate('electricity'),
          rentRefundDays: remainingDays,
          rentRefundEstimate,
          dailyRent: Math.round(dailyRent * 100) / 100,
          isEarlyTermination: remainingDays > 0,
          refundDeposit: depositAmount > 0,
          refundFireDeposit: fireDepositAmount > 0,
          refundLockCardDeposit: lockCardDepositAmount > 0,
          hasDeposit: depositAmount > 0,
          hasFireDeposit: fireDepositAmount > 0,
          hasLockCardDeposit: lockCardDepositAmount > 0,
          submitting: false
        }
      });
    }
    ```
    3. 新增 `closeSettlementDialog()`：
    ```typescript
    closeSettlementDialog() {
      this.setData({ settlementDialogVisible: false });
    }
    ```
    4. 新增 `handleSettlementRentDaysChange(event)` — 调整退款天数：
    ```typescript
    handleSettlementRentDaysChange(event: WechatMiniprogram.Input) {
      const days = Math.max(0, parseInt(String(event.detail.value || '0'), 10) || 0);
      const estimate = Math.ceil(days * this.data.settlement.dailyRent * 100) / 100;
      this.setData({
        settlement: {
          ...this.data.settlement,
          rentRefundDays: days,
          rentRefundEstimate: estimate
        }
      });
    }
    ```
    5. 新增 `toggleSettlementCheckbox(event)` — 切换押金勾选：
    ```typescript
    toggleSettlementCheckbox(event: WechatMiniprogram.BaseEvent) {
      const field = event.currentTarget.dataset.field;
      this.setData({
        settlement: {
          ...this.data.settlement,
          [field]: !(this.data.settlement as any)[field]
        }
      });
    }
    ```
    6. 新增 `confirmSettlement()` — 确认结算并结束租约：
    ```typescript
    async confirmSettlement() {
      if (this.data.settlement.submitting) return;
      const leaseId = this.data.detail?.activeLease?.id;
      if (!leaseId) return;
      
      this.setData({ 'settlement.submitting': true });
      try {
        await endLease({
          leaseId,
          settlement: {
            voidFutureSystemBills: true,
            rentRefundDays: this.data.settlement.rentRefundDays,
            refundDeposit: this.data.settlement.refundDeposit,
            refundFireDeposit: this.data.settlement.refundFireDeposit,
            refundLockCardDeposit: this.data.settlement.refundLockCardDeposit
          }
        });
        this.setData({ settlementDialogVisible: false });
        wx.showToast({ title: '租约已结束，结算完成', icon: 'success' });
        await this.loadDetail();
      } catch (error) {
        wx.showToast({ title: '操作失败，请稍后重试', icon: 'none' });
      } finally {
        this.setData({ 'settlement.submitting': false });
      }
    }
    ```
  </action>
  <acceptance_criteria>
    - `rg "settlementDialogVisible" miniprogram/pages/unit-detail/index.ts` 能找到
    - `rg "handleSettlementRentDaysChange" miniprogram/pages/unit-detail/index.ts` 能找到
    - `rg "confirmSettlement" miniprogram/pages/unit-detail/index.ts` 能找到
    - `rg "settlement:" miniprogram/pages/unit-detail/index.ts` 能找到参数透传
  </acceptance_criteria>
</task>

<task type="auto">
  <id>08-02-02</id>
  <title>创建结算面板 WXML 模板</title>
  <name>Task 2: 创建结算面板 WXML 模板</name>
  <files>miniprogram/pages/unit-detail/index.wxml</files>
  <read_first>
    - miniprogram/pages/unit-detail/index.wxml
    - .planning/phases/08-退租结算/08-CONTEXT.md
  </read_first>
  <action>
    在 `index.wxml` 的 `</view>`（页面最外层闭合）之前，新增对话框遮罩：
    ```xml
    <view wx:if="{{settlementDialogVisible}}" class="dialog-mask" catchtap="closeSettlementDialog">
      <view class="dialog-card settlement-dialog" catchtap="">
        <view class="section-subtitle">退租结算</view>
        
        <!-- 未收账单 -->
        <view class="settlement-section">
          <view class="settlement-label">
            未收账单：{{settlement.unpaidCount}} 笔
            <text wx:if="{{settlement.unpaidTotal > 0}}">，合计 ¥{{settlement.unpaidTotal}}</text>
          </view>
        </view>
        
        <!-- 水费提醒 -->
        <view class="settlement-section {{settlement.waterLastDate ? '' : 'settlement-warn'}}">
          <view class="settlement-label">
            水费最后记录：{{settlement.waterLastDate || '无记录'}}
            <text wx:if="{{!settlement.waterLastDate}}" class="warn-text"> ⚠️ 建议补录</text>
          </view>
        </view>
        
        <!-- 电费提醒 -->
        <view class="settlement-section {{settlement.electricityLastDate ? '' : 'settlement-warn'}}">
          <view class="settlement-label">
            电费最后记录：{{settlement.electricityLastDate || '无记录'}}
            <text wx:if="{{!settlement.electricityLastDate}}" class="warn-text"> ⚠️ 建议补录</text>
          </view>
        </view>
        
        <!-- 余下租金（仅提前退租） -->
        <view wx:if="{{settlement.isEarlyTermination}}" class="settlement-section">
          <view class="settlement-label">
            余下租金（预估）：¥{{settlement.rentRefundEstimate}}
            <view class="settlement-sub">每周期租金 ¥{{settlement.dailyRent}}/天</view>
          </view>
          <view class="labeled-input">
            <view class="field-label">退款天数</view>
            <input class="input" type="number" value="{{settlement.rentRefundDays}}" bindinput="handleSettlementRentDaysChange" placeholder="0"/>
          </view>
        </view>
        
        <!-- 押金 -->
        <view wx:if="{{settlement.hasDeposit || settlement.hasFireDeposit || settlement.hasLockCardDeposit}}" class="settlement-section">
          <view class="settlement-label">押金退还</view>
          <view wx:if="{{settlement.hasDeposit}}" class="settlement-check" data-field="refundDeposit" bindtap="toggleSettlementCheckbox">
            <view class="check-box {{settlement.refundDeposit ? 'check-box-on' : ''}}">{{settlement.refundDeposit ? '✓' : ''}}</view>
            <view class="check-label">押金（¥{{detail.activeLease.depositAmount || 0}}）</view>
          </view>
          <view wx:if="{{settlement.hasFireDeposit}}" class="settlement-check" data-field="refundFireDeposit" bindtap="toggleSettlementCheckbox">
            <view class="check-box {{settlement.refundFireDeposit ? 'check-box-on' : ''}}">{{settlement.refundFireDeposit ? '✓' : ''}}</view>
            <view class="check-label">消防押金</view>
          </view>
          <view wx:if="{{settlement.hasLockCardDeposit}}" class="settlement-check" data-field="refundLockCardDeposit" bindtap="toggleSettlementCheckbox">
            <view class="check-box {{settlement.refundLockCardDeposit ? 'check-box-on' : ''}}">{{settlement.refundLockCardDeposit ? '✓' : ''}}</view>
            <view class="check-label">门禁卡押金</view>
          </view>
        </view>
        
        <view class="settlement-actions">
          <button class="secondary-button" bindtap="closeSettlementDialog">取消</button>
          <button class="primary-button" loading="{{settlement.submitting}}" disabled="{{settlement.submitting}}" bindtap="confirmSettlement">确认退租结算</button>
        </view>
      </view>
    </view>
    ```
  </action>
  <acceptance_criteria>
    - `rg "settlementDialogVisible" miniprogram/pages/unit-detail/index.wxml` 能找到
    - `rg "退租结算" miniprogram/pages/unit-detail/index.wxml` 能找到
    - `rg "settlement-dialog" miniprogram/pages/unit-detail/index.wxml` 能找到
    - `rg "确认退租结算" miniprogram/pages/unit-detail/index.wxml` 能找到
  </acceptance_criteria>
</task>

<task type="auto">
  <id>08-02-03</id>
  <title>添加结算面板样式</title>
  <name>Task 3: 添加结算面板 WXSS 样式</name>
  <files>miniprogram/pages/unit-detail/index.wxss</files>
  <read_first>
    - miniprogram/pages/unit-detail/index.wxss
  </read_first>
  <action>
    在 `index.wxss` 末尾追加：
    ```css
    .settlement-dialog {
      max-height: 80vh;
      overflow-y: auto;
    }

    .settlement-section {
      padding: 16rpx 0;
      border-bottom: 1rpx solid #f0f0f0;
    }

    .settlement-section:last-of-type {
      border-bottom: 0;
    }

    .settlement-label {
      font-size: 28rpx;
      color: #333;
      line-height: 1.6;
    }

    .settlement-sub {
      font-size: 24rpx;
      color: #999;
      margin-top: 4rpx;
    }

    .settlement-warn .settlement-label {
      color: #dc2626;
    }

    .warn-text {
      color: #dc2626;
      font-size: 24rpx;
    }

    .settlement-check {
      display: flex;
      align-items: center;
      padding: 12rpx 0;
      gap: 16rpx;
    }

    .check-box {
      width: 40rpx;
      height: 40rpx;
      border: 2rpx solid #ccc;
      border-radius: 8rpx;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 28rpx;
      color: #fff;
      background: #fff;
      flex-shrink: 0;
    }

    .check-box-on {
      background: #07c160;
      border-color: #07c160;
    }

    .check-label {
      font-size: 28rpx;
      color: #333;
      flex: 1;
    }

    .settlement-actions {
      display: flex;
      gap: 20rpx;
      margin-top: 30rpx;
      padding-top: 20rpx;
      border-top: 1rpx solid #e5e7eb;
    }

    .settlement-actions button {
      flex: 1;
    }
    ```
  </action>
  <acceptance_criteria>
    - `rg "settlement-dialog" miniprogram/pages/unit-detail/index.wxss` 能找到
    - `rg "settlement-check" miniprogram/pages/unit-detail/index.wxss` 能找到
    - `rg "check-box-on" miniprogram/pages/unit-detail/index.wxss` 能找到
    - `rg "settlement-actions" miniprogram/pages/unit-detail/index.wxss` 能找到
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `rg -c "settlementDialogVisible\|confirmSettlement\|handleSettlementRentDaysChange" miniprogram/pages/unit-detail/index.ts`
- `rg "退租结算" miniprogram/pages/unit-detail/index.wxml`
- `rg "settlement-dialog" miniprogram/pages/unit-detail/index.wxss`
</verification>

<success_criteria>
- 点击结束租约弹出结算面板而非直接结束
- 面板展示未收账单、水电状态、余下租金、押金选项
- 退款天数可调整
- 确认后调用 endLease + settlement 参数
- 面板样式美观，复用现有设计模式
</success_criteria>
